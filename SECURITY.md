# Security Policy

## 威胁模型（先看这个，再判断是不是「漏洞」）

trip-expense-ledger 是一个面向**互相信任的出差同行人**的协作记账工具，不是面向陌生人协作的金融级系统。请求前先理解以下设计边界：

- **邀请链接认领机制不是金融级身份鉴权。** 它的目标是给同行人提供低摩擦协作方式（无需注册账号密码），不是抵御蓄意攻击者的强身份验证。如果你把邀请链接发给了不信任的人，本项目无法替你阻止对方冒充身份。
- **部署在 Cloudflare Workers 上，HTTPS/边缘网络层由 Cloudflare 托管**，不用自己套反向代理；但 Cloudflare 账号本身的访问控制（谁能进后台改 D1/R2/Worker）还是部署者自己的责任。
- **隐私边界是"同一个部署实例内，参与者之间"的边界**，不是抵御 Cloudflare 账号本身被入侵的边界。如果部署者的 Cloudflare 账号被攻破，本项目的应用层隐私控制无法保护数据。

在这个边界内，以下属于我们认为需要修复的安全问题：
- 未认领身份的用户能读取到本不该看到的他人消费明细
- 认领后的邀请链接仍然可以被重复使用来顶替身份
- session token 明文存储或可被预测
- 结算接口（唯一允许跨参与者读取的接口）意外泄露了不该公开的私密字段

## 报告漏洞

**请不要通过公开 GitHub Issue 报告安全漏洞。**

请选择以下任一私密渠道报告：

1. **GitHub 私密安全公告**（推荐）：在本仓库的 Security 标签页使用 "Report a vulnerability" 功能提交私密报告。
2. **邮箱**：发送到 koicreationstudios@gmail.com，标题请注明 `[SECURITY] trip-expense-ledger`。

请在报告中尽量包含：
- 问题描述 + 影响范围
- 复现步骤（如果可以，附上最小复现案例）
- 你认为的严重程度

我们会尽快确认收到并跟进修复。修复发布前请不要公开披露细节。

## 支持的版本

<!-- TODO(Remy): v0.1 发布后，视版本发布节奏决定要不要维护多个版本线；目前项目未正式发布，暂不设版本支持矩阵 -->

项目当前处于 v0.1 开发阶段，尚未有正式发布版本，安全更新会直接推到 `main` 分支最新版。
