# 复制为 .env 后按需修改，.env 本身不进 git（见 .gitignore）。
# D1/R2 不走环境变量，是 wrangler.jsonc 里配的 binding，本地开发靠
# next.config.mjs 的 initOpenNextCloudflareForDev() 自动接上本地 miniflare 模拟。

# session cookie 签名用的随机字符串，部署时用 `wrangler secret put SESSION_SECRET` 设置
# 生成方式: openssl rand -base64 32
SESSION_SECRET=change-me-to-a-random-value
