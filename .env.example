# 复制为 .env 后按需修改，.env 本身不进 git（见 .gitignore）

# SQLite 数据库文件路径（Docker 里默认挂载到 /data）
DATABASE_PATH=./data/db.sqlite

# 收据图片存储目录
RECEIPTS_DIR=./data/receipts

# session cookie 签名用的随机字符串，自托管时必须自己换成一个随机值
# 生成方式: openssl rand -base64 32
SESSION_SECRET=change-me-to-a-random-value

# 应用监听端口
PORT=3000
