# trip-expense-ledger 配色方向提案（2026-09-09）

> 这份文件不改 `DESIGN-BRIEF.md`，是独立的对比提案。`DESIGN-BRIEF.md` 里第三版到第八版定的圆角/字体分工/紧凑间距/emoji 图标语言/财务语义色（`emerald-600` 该收、`red-600` 该付）这些非颜色维度，两个方向都不重新讨论，等 Remy 选定方向后再回头看要不要跟着调。这份只谈"整体色相往哪个方向走"。

## 背景：为什么这次要跳出现有色系，不是再调一次 hex

`DESIGN-BRIEF.md` 从第三版到第八版，八轮修改全部发生在同一个色彩家族内部：藏青黑 `ink`（`#1A1A2E` → 柔和化后 `#23232E`）配低饱和金棕 `gold`/`gold-dk`/`gold-lt`、暖米白 `paper`，外加 `coral`/`seafoam` 两个语义色。中间调过深浅（第五版柔和化）、调过质感（第六版给 Hero 卡加渐变阴影）、调过是否要换成团队工具那种冷色调（brand-pm 铜棕 `#8B7248`、suimuse 近黑灰、team-board 墨绿 `#2f5d50` 三次都被否决，理由都是"团队协作工具的克制冷色不符合'温暖个人记账本'这个定位"）——但从没跳出过"藏青+金棕"这个底层组合本身。

Remy 这次看实机截图说"整个颜色我不喜欢"，说不清具体哪里，这是第一次出现指向"可能不是某个色号深浅的问题，是这整个色系本身的问题"的信号。第八版里 Remy 还说过一句"颜色感觉还是像 team-board"——团队看板真实主色是墨绿，跟这个项目的藏青金棕在 hex 上并不接近，我判断这句话不是在说色相真的撞了，是在说**深色 Hero 卡片 + 低饱和度**这个视觉效果，跟 team-board 那种冷静克制的"内部工具感"在气质上摸起来很像，哪怕具体颜色不同。这条不否定前八轮"不做团队工具冷色调"这个判断本身，那条判断依然成立；这次的两个方向都不会做成团队协作工具风格，只是要在"温暖有个性"这个大方向里，换掉"藏青+金棕"这一个具体配方。

## 我的判断：最可能在嫌弃什么

两个最可能的方向，分别对应下面两个提案：

1. **太暗太闷**——深藏青 Hero 卡经过第六版加渐变阴影、第五版专门柔和化过一轮，可能还是显得沉重压抑，像深色的文件夹或保险柜，不像"轻快的旅行记账本"。
2. **太素/没记忆点/显老气**——低饱和度金棕配藏青黑，这个组合本身容易被直觉读成"复古/传统"（像老式银行、老照片调色），跟"个人正在用的手机 App"这种新鲜感是两回事，配色个性不够鲜明，扫一眼记不住。

方向一对症"太暗"，方向二对症"太素/老气"。两个都提供是因为不确定 Remy 实际在意哪一种，给她挑。

---

## 方向一：暖阳明信片（Sunlit Postcard）—— 解决"太暗太闷"

**调性关键词**：明快、高饱和单色主打、像手写旅行日记本+登机牌，不是深色金融卡片。核心手法是"只留一个足够响亮的颜色，其余大面积留白/浅色"，不是"多种颜色一起调柔和"。

**参照**：
- **Monzo**（英国消费级银行 App，实测品牌主色 Hot Coral `#FF4F40`）——参照它"整套品牌只押一个高饱和度颜色，不掺任何第二个装饰色"这条纪律。Monzo 的克制不是靠调暗调灰，是靠"只有一种颜色，但这一种颜色够响"。这正好是现在这版八轮下来一直在做反面的事——现在的问题不是颜色太多，是把仅有的几个颜色都往低饱和、深色调，方向一是反过来往高饱和、亮色调走。
- **Wise**（跨境转账 App，实测品牌色 Lime `#9FE870` + Forest `#163300`）——参照它"重点色跟背景形成强对比"这个原则：Wise 的荧光青柠绿只在深色底上出现，两者对比强烈到一眼记住。方向一里 Hero 卡不再用深藏青底，改用高饱和度的暖色实底（下面色板的珊瑚橙），本身就是"强对比"这个原则的另一种落地方式，不需要靠深色才能对比强烈。
- **实体登机牌/护照章的暖调纸感**——不是网站参照，是物理对象参照：奶油卡纸底色、印章红/橙色的手写批注感，用来对照"这应该是随身带着用的东西"而不是"办公室金融软件"。

**核心色板**：
- 背景 `#FFF9F0`（比现在的 `paper #FEFCF7` 更亮更暖一档，去掉现在偏灰调的米白，换成更接近奶油卡纸的暖白）
- 主强调色（CTA / Hero 卡底色）`#FF6B45`（饱和珊瑚橙，取代现在的深藏青 `ink` 作 Hero 卡底色——Hero 卡从"深色沉字"改成"亮色实底"）
- 卡片底色 `#FFFFFF`（纯白卡片配 `#F3E4CE` 暖米边框，靠"卡片比页面更白更亮"制造层次，不是靠加深阴影）
- 语义色·该收/正向 `#2E9E4F`（清亮草绿，比现在的 `emerald-600` 更活一档）
- 语义色·该付/警示 `#E14B3E`（暖砖红，跟主强调色珊瑚橙同色系但明显更深更沉，两者放一起不会撞色，因为一个是"操作"一个是"警示"，靠明度区分而非色相区分）

**一句话卖点**：把 Hero 卡从"深藏青实底压字"换成"高饱和珊瑚橙实底"，用响亮的暖色而不是深色去做视觉重量，解决"暗/闷/压抑"这个感受。

---

## 方向二：清爽薄荷（Fresh Mint）—— 解决"太素/显老气"

**调性关键词**：干净、清新、当代旅行钱包 App 的气质，不是复古文件夹，也不是团队看板的冷灰。这个方向是唯一一处真正换掉色相家族的提案——从"藏青+金棕"暖旧色系，整个换成"薄荷青+白"。

**参照**：
- **N26**（德国数字银行 App，实测品牌色 Teal `#088177`、petrol 深青 `#2b697a`）——参照它"用一个饱和青色贯穿全站主色，配纯白/浅灰底"这套配方。N26 属于欧洲新一代数字银行 App 的典型代表，青色系是它们这一类 App 的共同选择（跟传统银行的深蓝/暗金刻意拉开区别），这正是 trip-expense-ledger 需要的"新鲜感"。
- **Wise**（同上，`#163300` 森林绿 / `#9FE870` 青柠绿）——这次参照的是它证明"绿色系金融 App 完全可以显得现代、不显老气"，用来反驳"薄荷/青绿会不会看起来廉价"这个顾虑；但**明确不采用它深色森林绿做大面积底色的手法**（那接近方向一要避开的"深色沉重"问题），方向二只取它的清亮青柠绿做点缀强调色。
- **刻意排除、并说明为什么不是同一回事**：`DESIGN-BRIEF.md` 第七版否决过的 team-board 墨绿 `#2f5d50`——那是**暗森林绿**，用在"团队协作看板"上传达的是"严肃/内部工具"。方向二这里用的是**亮薄荷青**（下面色板 `#0E8A78`）配纯白底，明度和调性都跟暗森林绿相反，是"清新活泼"不是"沉稳克制"，这不是同一个方向换个名字重提，是刻意选了同一色相里最不像 team-board 的那一段。

**核心色板**：
- 背景 `#FAFBF9`（接近纯白但留一点点冷调，比方向一冷、比现在的暖米白 `paper` 冷，是三个方向里唯一"冷白"的一个）
- 主强调色（CTA / Hero 卡）`#0E8A78`（清亮薄荷青，比 N26 的 `#088177` 略提亮一档，避免过深读成 team-board 那种暗绿）
- 卡片底色 `#FFFFFF`，边框 `#D6EDE7`（浅薄荷灰绿，取代现在的暖米色 `sand` 边框）
- 语义色·该收/正向：沿用主强调色的深一档 `#0B6B5C`（因为主色系本身就是绿色，"该收"用同色系深一档而不是另开一个绿，靠明度区分"操作"和"结果"）
- 语义色·该付/警示 `#E2574C`（暖珊瑚红，跟冷色主调形成暖冷对比，红色在满屏青绿里会非常显眼，警示效果比现在"金棕底配珊瑚红"更强）

**⚠️ 需要在下一步一并想清楚的连带影响**：现在色板里 `seafoam #2DAA85`（专门标"比价最划算"）本身就是薄荷绿。如果选方向二，主强调色跟这个语义色会撞到同一个色相，"比价最优"这个徽章需要重新挑一个不跟主色冲突的颜色（比如金黄或亮橙做"最优"高亮），这个调整这份文件先不展开，等方向二真的入选再处理。

**一句话卖点**：整个跳出藏青金棕这个暖旧色系，换成当代数字银行 App 常用的青绿+白，解决"太素/显老气/没记忆点"这个感受，缺点是跟项目已经建立的 `seafoam` 语义色需要联动调整。

---

## 两个方向怎么选

- 如果 Remy 的核心感受更接近"太暗太压抑"，选**方向一**（暖阳明信片）——保留"暖色调"这个大方向不变，只是把深色低饱和换成亮色高饱和。
- 如果核心感受更接近"看着老气/没个性/说不出哪里怪"，选**方向二**（清爽薄荷）——这个改动更大，连色相家族都换了，但也更有把握制造"一眼不一样"的效果。
- 也可以两个都做成可切换的静态对比页面（不需要先改真实代码），让 Remy 直接在同一批截图上左右对比，比单纯读色值表更容易做判断——这条留给下一步实现视觉样例时考虑，这份文件只出方向不出实现方式。

---

# 第三提案：复古海边度假明信片（基于 Remy 提供的参照图，非多选一）（2026-09-09）

> **这次的性质，先说清楚跟上面两个方向的关系**：上面方向一/方向二是我在没有具体参照的情况下，猜"Remy 可能在嫌弃什么"给出的两个假设性方案，都已经被否决。这次 Remy 直接给了一张具体的参照图（一份叫"Summer Breeze / Peaceful Retreats"的旅行度假品牌视觉规范板），不再需要猜方向，只需要把这张图翻译成项目能用的具体 token。所以这次不是"方向三选一"，是唯一一版方案，不摆多个选项。
>
> **关于参照图这个东西本身**：我查过了，"Summer Breeze / Peaceful Retreats"不是一个能查到官方色卡的真实产品或品牌（大概率是 Canva/Etsy 一类的品牌视觉规范模板），网上搜不到权威 hex 值可以核对，所以下面的色值以 Remy 目测报的数字为基准去做微调，不是我自己另外核实过的"真实数据"。这跟第三版参照 remy-thailand 时"curl 线上页面拿真实 CSS"那种可验证的参照不是同一个可信度级别，这里明确说清楚。

## 先纠正一处："该收目前用的是 seafoam 薄荷绿"这句不准确

翻了 `DESIGN-BRIEF.md` 第三版"视觉决定"和第五版第 4 条，`seafoam #2DAA85` / `sf-lt #D0F0E5` 专门标"比价最划算"这一个语义（换汇渠道比价卡片的最优高亮），从第三版定案起就跟"该收/该付"这条财务语义色**刻意分开**（原文："这是两件不同的事，一个是'我跟同行人之间欠多少钱'，一个是'这笔消费选哪个支付方式更省'。共用 emerald 会让用户扫视页面时把两种不同性质的绿色高亮混成一回事"）。真正管"该收/该付"的，从第一版起就是 Tailwind 默认色 `emerald-600`（该收，#059669）/ `red-600`（该付，#DC2626），八轮改版里反复强调"财务语义色最不该轻易动"的就是这两个，不是 seafoam。

这条不是抠字眼，是怕这个误解被 frontend-dev 当成事实执行下去，改错了颜色。下面我按真正的定义（`emerald-600`/`red-600`）来判断要不要换，`seafoam` 这次不在讨论范围内，保持第三版定案不动。

## 参照图翻译成具体决定

### 1. 背景（对应现有 `paper`）

参照图暖米白纸感，Remy 目测 `#F5F0E1` 附近。这个值跟现有 `paper #FEFCF7` 比，暖度和"纸感"（更低的明度、更明显的黄褐调）都更强，符合"比现有 paper 更暖更有质感"这个要求。我按这个目测值微调了一点让色调更干净（去掉一点点偏绿的杂色），定为 **`#F5EFDD`**，肉眼几乎看不出跟 Remy 报的值有差别，方向完全没偏。

### 2. Hero 净额卡主色（对应现有 `ink`，取代现在的近黑藏青）

这是这次最核心的一处判断，展开算给 Remy 看依据，不是凭感觉调的。

现有 `ink #23232E` 换算成 HSL 是 **H240° / S14% / L16%**——色相虽然落在"藏青"这个区间，但饱和度只有 14%，在这么低的明度下几乎读不出"蓝"这个色相，肉眼看就是"很深的灰黑"，这正是 Remy 反复反馈"没感觉""像 team-board"背后的一个具体成因（色相被饱和度和明度一起吃掉了）。

参照图目测值 `#1E4B78` 换算成 HSL 是 **H210° / S60% / L29%**——饱和度是现有 `ink` 的 4 倍多，明度也提了将近一倍，色相还往纯蓝方向挪了 30°（240°偏紫的藏青 → 210°更接近海蓝的藏青）。这三个变化加起来，才是"真正显色，不再显黑"这句话的具体数值含义，不是简单调亮一点点。

配白字文本对比度实算约 **9:1**（WCAG AA 正文要求 4.5:1，AAA 要求 7:1），比现有 `ink` 的可读性只会更好不会变差，不用担心换成更亮的藏青会牺牲可读性。

**结论：Hero 卡/CTA/选中态主色定为 `#1E4B78`**，直接采用 Remy 的目测值，不用再调（这个色号已经算过对比度，方向和数值都站得住）。**这次仍然是"藏青"这个色相家族，没有像方向一（换成珊瑚橙）、方向二（换成薄荷青）那样跳到完全不相干的色系**——跟任务描述的要求一致。

### 3. 卡片底色/边框（对应现有的卡片 `bg-[#EDE8DA]/35` 叠加 + `sand` 边框）

参照图给的第三个元素是"雾灰蓝 `#9BABC0`"，描述成"柔和过渡色"。这次借它做卡片边框，理由是这样能在暖米背景和深藏青 Hero 卡之间，做出一处冷色调的呼吸，是那种老式海报（暖米纸+深藏青印刷+一点冷调天空蓝）常见的暖冷对比手法，比继续用现有的暖米色 `sand` 边框（背景、卡片、边框三者都是暖色）更有层次。

卡片底色改成**纯白 `#FFFFFF`**（呼应参照图"邮票齿边卡片"那种白色卡纸质感），边框把参照图的雾灰蓝 `#9BABC0` 提亮到卡片边框适用的浅色调，算出来是 **`#D9E3ED`**（跟原始雾灰蓝同色相同饱和度，只是把明度从中等提到跟现有 `sand` 差不多的浅色区间，这样卡片边框不会因为太深而抢戏）。

**结论：卡片底色 `#FFFFFF`，边框 `#D9E3ED`**。现有 `sand #EDE8DA` 这个 token 名字继续用，只是把 hex 值换掉（跟第五版处理 `ink` 换值时同一个做法，不改 token 名字，改动面最小）——换完之后 `sand` 这个名字会有点文不对题（原意是"沙"，现在存的是雾蓝灰），这个纯粹是命名层面的小瑕疵，值得记一笔但不值得为了改名去动全站几十处 `border-sand`/`bg-sand` 引用，这个决定留给 frontend-dev，不强制。

顺带一提：参照图里雾灰蓝的原始饱和度（不提亮的那版 `#9BABC0`）其实也可以留着做别的用途，比如替换现有 `muted #8A7A6A` 这个次要文字颜色，让它也带一点冷调呼应。这个不在这次 5 个核心 token 的范围内，只是留个观察，供以后细化时参考。

### 4. 该收（正向）

给两个选项，说清楚各自的理由：

**选项 A（零改动成本）：维持现有 `emerald-600 #059669` 不变。** 这个颜色本身不跟新色板里任何一个颜色冲突（不撞色相、不撞语义），继续用没有任何技术代价（`text-emerald-600` 这类 Tailwind 内置类名不用改一行代码）。

**选项 B（我的推荐）：换成暖调橄榄绿 `#4D7D36`。** `emerald-600` 是偏蓝调的青绿色（HSL 大约 H160°），这个色相跟"复古海边度假明信片"这套暖色为主的搭配放在一起，会显得比周围颜色更"冷/干净/现代"，跟整体的暖旧复古调性有点脱节——有点像在一张老照片上贴了一个新贴纸。橄榄绿/棕榈叶绿是经典复古旅行海报里常见的绿色（配藏青、赤陶、米白正好是那种地中海/热带旅行海报的四色组合），色相落在 H100° 左右，跟"该付"要用的赤陶红、跟现有的 `seafoam`（H162°）都拉开了足够的距离，不会出现"两个绿分不清"这种坑（这份简报之前吃过好几次这个亏：`gold` vs brand-pm 的 `accent`、`gold-dk` vs brand-pm 的 `wait`，都是"两个颜色太像被当成两个不同变量"栽的跟头，这次选绿色的时候特意把这条教训用上）。配白底文本对比度实算约 4.9:1，够用（WCAG AA 门槛 4.5:1）。

**代价说清楚**：选项 B 需要把项目里所有 `text-emerald-600`/`bg-emerald-*` 这类硬编码引用换成一个新 token（比如 `positive #4D7D36`），是有实际改动量的，选项 A 是零成本。**我倾向选项 B**，因为这次改配色的初衷就是解决"太素/没记忆点/跟整体不协调"这类感受，emerald 这个 Tailwind 默认色本身也符合第三版"绝对禁止"里点名过的"配色是看起来专业的默认色、没有自己的品牌色"这个坑，只是这次它撞上的不是蓝紫渐变那种典型 AI 脸，是没被人挑过的默认工具色——性质上是同一类问题。但这条决定权重不小（動到全站财务语义色），最终选 A 还是 B 交给 Remy 定。

### 5. 该付（警示）

直接采用参照图的砖红/赤陶色 **`#C1432C`**，这个不需要额外判断，它本来就是参照图里明确标出来给"该付/警示"这类语义用的颜色（图上原话是"太阳纹图标色"，但赤陶红在旅行海报配色里本来就是最典型的"警示/强调色"角色，跟"该付"这个语义天然贴合）。配白底文本对比度实算约 5.1:1，通过 WCAG AA。

现有 `red-600 #DC2626` 是 Tailwind 默认红，跟 `#C1432C` 比，默认红更"标准警报红"，赤陶红更暖更沉，跟整体暖旧色调更协调，这条判断不需要像"该收"那样纠结，直接换。

## 核心色板汇总（跟前两个方向同样的 5 项结构，方便 Remy 对比）

- 背景 `#F5EFDD`（暖米卡纸感，比现有 `paper #FEFCF7` 更暖更有质感）
- 主强调色（Hero 卡 / CTA / 选中态，取代现有 `ink #23232E`）`#1E4B78`（正藏青蓝，饱和度是现有 ink 的 4 倍多，配白字对比度约 9:1）
- 卡片底色 `#FFFFFF`，边框 `#D9E3ED`（雾灰蓝提亮版，取代现有暖米 `sand` 边框，做暖冷对比）
- 语义色·该收/正向：选项 A 维持 `emerald-600 #059669` 不动（零成本）／选项 B（推荐）换成暖调橄榄绿 `#4D7D36`（更贴合整体暖旧调性，但需要改代码）
- 语义色·该付/警示 `#C1432C`（参照图原色，直接采用，替换现有 `red-600 #DC2626`）

## 字体：Fraunces 能不能撑起"优雅衬线大标题"这个角色

**能，字体能力上没问题，不需要引入新的衬线字体家族。** Fraunces 本身就是为大字号展示场景设计的暖调怪奇衬线体（不是那种偏中性的正文衬线体），当前项目已经加载了 300-700 五档字重 + 正常/斜体，这个范围完全撑得起参照图"Summer BREEZE"那种复古海报大标题的气质。

**但撑不起来的真正原因不是字体能力，是内容语言限制，这条纪律这次不能松。** "绝对禁止第 6 条"从第三版起就锁死 Fraunces 只能用在阿拉伯数字上，因为这套字体不含中文字形，套上中文会静默回退到系统默认衬线体（通常是宋体），两种字体会在页面上打架。参照图的"Summer BREEZE"是纯英文品牌词，这个前提在 trip-expense-ledger 不成立，因为项目里几乎所有真实标题内容（行程名、分节标题）都是中文。

**结论：Fraunces 继续严格限定在数字金额场景，不碰中文标题。** 这次配色换血之后，"让 Fraunces 更好地扮演优雅大标题角色"的正确落地方式，是让它在已经被允许出现的场景（净额数字、金额）里发挥得更饱满（比如第六版已经开始尝试更大字号+更重字重的 Hero 数字），不是让它去碰中文。中文标题的个性继续靠字号 + 主色（`gold-dk` 或换成这次新引入的 `#1E4B78`，这条不在这次 5 个核心 token 范围内，留给下一步细化时判断）去做，字体家族不换。

## 手写体点缀：可以引入，但先说清楚现状——项目里目前没有能合法用它的地方

参照图"soulful travel"那种连笔斜体手写字，如果要在这套系统里落地，**推荐 Google Fonts 的 `Parisienne`**——优雅纤细的连笔手写体，是复古度假明信片/请柬类设计里最常被选用的字体之一，笔画克制不花哨，跟参照图气质贴合。备选 **`Yesteryear`**——手绘海报感更浓、字形更粗犷，如果想要更接近 1930 年代旅行海报那种拓印手写感，可以选这个替代。跟 Fraunces/Plex Mono 一样，**这个字体一旦引入必须严格限定只用在纯英文/数字场景**，绝对不能碰中文（同"绝对禁止第 6/14 条"那条纪律，这是第三个套用同一条限制的字体，不是新开一个例外）。

**但要诚实说一句**：目前这个项目的 UI 上没有任何一处合法的英文短句/情绪文案落地点——`app/layout.tsx` 里唯一的英文只有浏览器标签页标题（用户看不到），页面上所有真实展示文案都是中文，连一个英文品牌词/口号都没有。这跟 Fraunces（现成的、随处可见的金额数字场景）、Plex Mono（现成的货币代码/邀请码场景）不是一回事——手写体字体目前没有一个"天然就该用它"的位置。如果 Remy 确定要引入，需要先想好具体用在哪一句英文短语上（比如 Hero 卡水印区域除了货币符号之外加一行极小的英文点缀语，或者未来做"行程分享卡/导出图"这类真正模仿明信片的场景），不建议为了"参照图有所以我们也要有"就现造一个用不上的字体文件占着体积。**这条列进下面"加分项"，不是这次的必做项。**

（技术侧提醒，供 frontend-dev 参考，不是这份简报要钉死的事：这个项目现有三个字体家族全部走 `next/font/google` 构建时打包，理由是"数据自托管、不外流"这条调性（`app/layout.tsx` 里的原文注释）；新引入的手写体字体如果要落地，也要走同一条路线，不能用 `<link>` 外部 CDN 引入。）

## 装饰元素：邮票齿边卡片 + 贝壳/太阳纹线稿图标——加分项，不是核心方案的必需部分

这两样是参照图里比较抢眼但也比较"重"的装饰手法，单独说清楚要不要做：

**邮票齿边（scalloped edge）卡片**：如果要做，建议只用在 1-2 个真正想强调"这是一张精美小卡片"的场景（比如 Hero 卡的角标装饰，或者未来如果做"行程分享/结算导出图"这类真正模仿明信片的场景），不建议全站卡片都套上——这份简报从第三版起就有一条纪律，"每处最多一个装饰重点"，全站铺开齿边会变成喧宾夺主，反而稀释了它本该有的"这张特别精美"的效果。技术上可以用 `radial-gradient` 做 mask 或 SVG `clip-path` 实现，具体做法交给 frontend-dev。

**贝壳/海星/太阳纹线稿小图标**：如果要做，建议当成独立于现有 emoji 图标语言之外的纯装饰层（比如页面留白角落的点缀，不承担任何信息识别功能），不要跟现有的 emoji 图标混着用去表示"这是什么类型的钱包/记录"——两套图标语言承担的角色不一样，混用会让"图标到底代表什么"变得含糊。同样遵守"每处最多一处，不要堆砌"这条纪律。

**这两样都不是这次核心配色方案要不要成立的必要条件**，色板/主色/字体那几条判断即使不做这两样装饰也完全成立，是锦上添花的加分项，Remy 可以单独决定要不要做、什么时候做。

## 这次没有改动的东西（跟前两版一样，明确写清楚范围边界）

- 紧凑尺寸基准（`user_compact_ui_preference.md`，第四/六版已经落地的字号/padding/触控热区）——这次是纯粹的色彩/字体方向提案，一个尺寸数字都不碰
- 圆角尺度、emoji 默认图标语言、Plex Mono 的使用边界（只能碰纯 ASCII 场景）——沿用现有纪律，不重新讨论
- `seafoam`/`sf-lt`（比价最划算）、`ok`/`ok-bg`/`live`/`live-bg`（状态色体系）、`wait`（复用 `gold-dk`/`gold-lt`）——这几个语义色这次不在讨论范围内，等这版主色板方向定下来之后，如果 Remy 觉得它们也需要跟着新主色调整（比如 `gold` 系是不是也要往参照图的"淡黄奶油黄 `#EFDFA0`"靠一靠——这个色号其实已经很接近现有 `gold-lt #F0E8D6`，大概率不需要大改，但这个判断留到下一轮）
- Fraunces 只用数字金额、Inter 管中文这条纪律本身——不变，这次只是重新论证了一遍为什么这条纪律在新方向下依然成立

这份提案只是方向确认，实现前建议按第三版一贯的做法，先出一版静态视觉样例（截图或 artifact 对比页）让 Remy 确认色板/字体没走偏，再动真实代码。

---

# 第四提案：藏青提亮 + 大色块呈现（基于 Remy 二选一确认"两个都是"）（2026-09-09）

> **这次要解决的是第三提案的两处问题**：Remy 反馈第三提案"底色太暗"；中间理解错方向、改成"浅色卡片配藏青大字"后被明确否决（"不要这个"/"这个版本我不喜欢"）；直接问了 Remy 一个二选一——"问题是色号本身太深太闷，还是呈现方式太小气（该做成真正大面积色块）？"——**Remy 选的是"两个都是"**。所以这版要同时给出：①一个比 `#1E4B78` 更亮更响的藏青候选，附完整 HSL/对比度分析；②一个"真正大面积色块"的具体呈现方案，参照 howdystudioco 给 coco_creativeagency 做的网站截图（整块饱和色实底配简洁白色线条图标）。
>
> **关于 howdystudioco 参照图的可信度，先说清楚**：这张图 Remy 只用文字描述转达（构图是几个区块并排——暖米区块 + 一整块饱和藏青实底配白色线条图标的区块 + 蓝白细条纹区块），这边没有截图文件、也搜不到这张具体 Instagram 帖子的原图，我没法像第三版参照 remy-thailand 那样拉真实 CSS 逐值核对。下面的呈现方案是照 Remy 转达的文字描述去翻译构图逻辑，不是我亲眼核实过图片细节的可信度，这条跟第三提案开头对"Summer Breeze"模板的免责声明是同一个性质，不重复强调第二次。

## 1. 色号判断：藏青要多亮才够，不是拍脑袋调

### 1.1 先回应"要不要换掉藏青这个色相家族"

**不换。** 判断依据：Remy 两次确认过的信号里，都没有否定"藏青"这个色相本身——第三提案 `#1E4B78` 被否的理由是"底色太暗"（明度问题），后来被否的"浅色卡片配藏青大字"版本被否的理由是"呈现方式"（Remy 原话没有指向色相），这次二选一给出的答案是"两个都是"，对应的正是"太深太闷"（明度/饱和度问题）+ "太小气"（呈现方式问题），依然没有一次否定过"藏青"这个色相家族本身。加上第三提案已经论证过"复古海边度假明信片"这套暖米+雾蓝灰+藏青的组合逻辑是成立的（`#F5EFDD` 背景配藏青主色的暖冷对比这套关系没被否决过），换色相家族会让第三提案里"背景/边框/该收/该付"那几个已经站住的判断全部要推倒重来，性价比不高。**这次的判断是：留在藏青家族里，只调亮度/饱和度这两个维度，不动色相。**

### 1.2 现有候选 `#1E4B78` 的问题量化

`#1E4B78` = H210° / S60% / **L29%**，配白字对比度约 9:1。对比度数字本身没问题（远超 WCAG AAA 门槛），但 **L29% 这个明度值确实偏深**——作为参照，人眼把一个颜色读成"鲜活/有精神"而不是"深沉/厚重"，明度通常需要落在 35%-45% 这个区间（这也是下面两个候选刻意往这个区间挪的原因，不是随便选的数字）。L29% 卡在"看得出是蓝但仍然偏暗"这个不上不下的区间，这就是"太深太闷"这个反馈的具体数值成因。

### 1.3 两个候选：只调亮度/饱和度，色相锚定不动

**候选 1（稳健提升，推荐先看这个）：`#1D5FA8`**
- HSL：H212° / S71% / **L39%**（对比 `#1E4B78`：色相基本不变 +2°，饱和度 +11pt，明度 +10pt）
- 配白字文本对比度实算约 **6.45:1**（WCAG AA 正文门槛 4.5:1，仍稳稳过关，只是没有 `#1E4B78` 的 9:1 那么夸张——这是提亮必然要付出的代价，用更亮的颜色去换更强的"有活力"观感，对比度往下让一点点是合理交换，不是缺陷）
- 视觉判断：色相几乎没动（210°→212°），所以看起来仍然是"同一个藏青，只是更响了"，不会让人觉得换了一个完全不同的颜色，风险最低

**候选 2（更响亮，如果候选 1 试完 Remy 还嫌不够冲击力再上这个）：`#1C64C2`**
- HSL：H214° / S75% / **L44%**（对比 `#1E4B78`：色相 +4°，饱和度 +15pt，明度 +15pt）
- 配白字文本对比度实算约 **5.75:1**（仍过 WCAG AA 正文门槛，余量比候选 1 少一些，但因为 Hero 卡的净额数字本来就是 `font-serif text-2xl font-medium` 这种大字重展示字号，即使按更严格的"大字文本"门槛 3:1 衡量也绰绰有余）
- 视觉判断：这是我在"藏青"这个色相家族里能给出的提亮上限——继续往上加饱和度/明度，会开始逼近下面 1.4 节说的"通用科技蓝"边界，色相家族会开始变得不像"藏青"，更像"标准蓝"

### 1.4 提亮的边界在哪：用两个真实存在的蓝做锚点，划出"还没滑成通用蓝"的线

选这两个候选不是凭感觉停在某个数字，是拿两个大家一眼就能认出来的"标准科技蓝"做边界参照，确保候选值不会不小心滑进"绝对禁止清单"点名过的那类默认感：
- **Facebook 蓝 `#1877F2`**：H214° / S89% / L52%
- **Tailwind `blue-600` `#2563EB`**：H221° / S83% / L53%

这两个是被普遍认成"最没有品牌个性的默认科技蓝"的两个真实案例（Tailwind 默认色本身就是第三提案"绝对禁止"清单里点名过的坑之一）。它们的共同点是**明度都在 52-53%，饱和度都在 83-89%，色相都在 214-221°**——比候选 2 的 L44%/S75%/H214° 还要再亮再艳一档，色相也更往"纯蓝"偏移。候选 1、候选 2 都刻意停在这两个锚点的下方，这样才能保证提亮之后视觉上读出来的仍然是"更有精神的藏青"，不会一脚踩进"看起来像随便拿了个 Tailwind 默认蓝"这个雷区——这正是这份简报"绝对禁止"清单最需要提防的那类翻车。

### 1.5 结论

**Hero 卡/CTA/选中态主色定为候选 1 `#1D5FA8`**，作为这版的默认落地值；**候选 2 `#1C64C2` 留作备选**，如果 Remy 看完候选 1 的实机效果，觉得还不够"响"，再直接换成候选 2（两者色相同源，替换只是改一个 hex 值，不牵动其它 token）。

## 2. 呈现方式判断：从"卡片"改成"真正的大色块"

### 2.1 现状问题定位

现在 Hero 净额卡（`app/trips/[tripId]/page.tsx` 第 86-111 行）是一个 `rounded-hero`（22px 圆角）、四边都留在 `mx-auto max-w-3xl px-4` 容器内的卡片，本质上跟页面上其它卡片（钱包卡、参与者列表）是同一种"容器语言"，只是颜色更深。这正是"呈现方式太小气"的具体成因——不管颜色多亮，只要它还是"卡在容器边距里的一个圆角小方块"，视觉分量就上不去，够不到参照图里"整块区域是纯色实底"那种气势。

### 2.2 方案 A（首选）：满宽通栏色块，贴齐屏幕边缘

- **做法**：Hero 区块从"卡在容器内的圆角卡片"改成左右贴齐屏幕边缘的通栏色块——技术上是让这一块突破 `app/layout.tsx` 里 `mx-auto max-w-3xl px-4` 这层容器的左右内边距（常见做法是给这个区块套负 margin 抵消父容器的 `px-4`，具体用负 margin 还是拆分布局结构，这属于实现细节，交给 frontend-dev 判断，这份简报只钉"视觉效果要做到贴边"这个结果）。
- **⚠️ 不加大内部元素尺寸，只加宽外框**：内部 padding（现有 `p-4`）、净额数字字号（`text-2xl`）、标签字号（`text-[10px]`）这些全部维持不变，"色块要大"完全靠宽度突破容器边界来实现，不靠放大内部元素。**这条特别标出来，是因为第四轮 DESIGN-BRIEF 就是靠放大字号/留白做"视觉分量"被 Remy 打回来过**（`user_compact_ui_preference.md` 记的教训：净额卡片的巨大 Fraunces 数字被反馈"太夸张"，全站"全部东西都很大很占位子"）。这次做大色块是"宽度"维度的决定，跟"紧凑基准"管的是"内部元素尺寸/留白密度"这个维度完全不冲突，两者不矛盾，但如果实现时顺手把字号也跟着放大，就是重犯同一个错误，这里明确划清界线。
- **圆角调整**：贴边之后，原来四角统一 `rounded-hero`（22px）的处理方式要跟着改——贴到屏幕两侧的那两条边圆角去掉（改直角，或极小的 4px 左右），只在色块跟上方标题/下方内容之间的过渡处酌情保留圆角，不然贴边的两侧还留着大圆角会显得色块边缘"浮"在容器里，跟贴边的意图自相矛盾。具体数值交给 frontend-dev 判断哪个观感更顺。
- **图标**：现有那个 140px 半透明衬线货币符号水印（`text-white/[.05]`，几乎看不见的装饰纹理）换掉，改成一个真正看得清的白色线稿图标，尺寸约 40-56px、描边粗细约 1.5px，摆在色块右下角，不透明度提到 30-40%（不是现有水印那种 5%）。图标内容推荐 lucide-react 已有的 `Coins`（硬币堆叠线稿，呼应"钱"这个主题，不会被误读成专门指向"该收"或"该付"任一方向）；备选 `ArrowRightLeft`（双向箭头，呼应净额"有来有回"的概念，但要注意这个图标在很多 App 里也常被用来表示"同步/切换"，可能被误读，如果选这个要在旁边配色块本身的净额数字一起看才不会歧义）。项目已经装了 `lucide-react`（`package.json` 第 27 行），不用新增依赖。
- **跟"每处最多一个装饰重点"这条纪律的关系**：现有的水印和新图标不能同时留（会变成两个装饰元素打架），这次是**替换**不是**叠加**，去掉水印，只留新图标。

### 2.3 方案 B（备选，改动面更小）：维持容器宽度，去掉圆角+贴顶留白

如果方案 A 的贴边通栏对 frontend-dev 来说牵动布局结构太多（要动 `app/layout.tsx` 或做负 margin 处理），退一步的方案：色块继续留在 `max-w-3xl` 容器内（宽度不变，现状已经是 `w-full`），但把圆角去掉改成直角矩形，并且收紧色块跟页面标题之间的间距（现有 `flex flex-col gap-6` 给了很宽松的呼吸感，可以把这一处的间距单独收窄，让色块贴着标题往下焊死），靠"直角+零呼吸感"制造"这是页面结构的一部分"而不是"漂浮的卡片"这种视觉效果。

这个方案在手机端（本项目的主要使用场景，`layout.tsx` 里 `RecordExpenseFab` 单手持机的设计取向已经说明这一点）跟方案 A 观感其实很接近，因为 `max-w-3xl`（768px）本身就已经接近手机屏幕宽度；主要差别在桌面端——方案 B 在桌面端还是会被容器宽度限制住，达不到参照图那种"占满整个视觉区域"的气势。**如果开发资源允许，优先选方案 A**；方案 B 是纯粹为了控制改动成本准备的退路。

### 2.4 这次不打算做的：literal 复刻参照图的"三区块并排"构图

参照图的原始构图是三个区块并排（暖米区块 + 藏青实底区块 + 蓝白细条纹区块），但这个构图适合的是有多组独立内容要并列展示的场景（比如营销落地页的服务分类入口）。trip-expense-ledger 这个 Hero 区块现在实际承载的信息只有一个"净额数字"加一条"查看结算明细"链接，如果为了照抄三区块构图硬造出第二、第三个区块去填内容，会变成"为了像参照图而制造不存在的信息"，这个不划算。**这次只取参照图"整块饱和色实底配简洁白色线条图标"这一个核心构图逻辑，不照搬"多区块并排"这个具体排布**——这条判断是我的取舍，没有跟 Remy 单独确认过，如果 Remy 看完方案 A/B 还是想要更接近参照图的并排感，需要先想清楚具体拿什么内容填第二、第三个区块，再回头讨论。

## 3. 沿用第三提案没被否决的部分（不重新论证，直接继承）

| Token | 第三提案定案值 | 这版是否变动 |
|---|---|---|
| 背景 `paper` | `#F5EFDD` | 不变，直接继承 |
| Hero 主色（取代 `ink`） | `#1E4B78` | **这版调整为候选 1 `#1D5FA8`（默认）/ 候选 2 `#1C64C2`（备选）**，见第 1 节 |
| 卡片底色 | `#FFFFFF` | 不变，直接继承 |
| 卡片边框（取代 `sand`） | `#D9E3ED` | 不变，直接继承 |
| 该收/正向 | 选项 A `emerald-600 #059669`（零成本）／选项 B（推荐）`#4D7D36` | 不变，两个选项都继续有效，交给 Remy 定 |
| 该付/警示 | `#C1432C` | 不变，直接继承 |
| Fraunces 使用边界 | 只用阿拉伯数字，不碰中文 | 不变，直接继承 |
| Hero 呈现方式 | （第三提案未涉及，沿用现状圆角卡片） | **这版新增：改成大色块，方案 A（贴边通栏）或方案 B（容器内直角+收紧留白）**，见第 2 节 |

这份提案只出方向，实现前建议先出一版静态视觉样例（候选 1 藏青 + 满宽色块 + 新线稿图标，一起截一张实机模拟图）让 Remy 确认没走偏，再动真实代码。

---

# 第五提案：自然草木调（基于 Remy 提供的 Pinterest 配色卡，非多选一）（2026-09-09）

> **这次要解决的问题**：Remy 看完第三/四提案的实机模拟对比页，原话是"整体颜色我还是不喜欢"——藏青这整个方向到此为止，不再在这个色相里继续调。Remy 甩了一张新的参照图（Pinterest 配色卡，账号 souline_by_ana，白色兰花+青柠切片在浅灰泥墙背景上，叠加四条纵向色块）：Wild Thyme `#494f2f`（深橄榄绿）、Lemon Sage `#9b9856`（中橄榄黄绿）、Porcelain Mist `#e1e2dc`（极浅灰绿白）、Desert Linen `#e0dac2`（暖沙米色）。
>
> **这次只取这四个颜色本身的气质，不照抄参照图的构图**——"兰花+青柠+纵向堆叠色块"是那张 Pinterest 图自己的呈现方式，不是 trip-expense-ledger 该长的样子，这份提案只把四个颜色翻译成项目实际用得到的 token。所以这次跟第三/四提案一样，不是"方向三选一"，是唯一一版方案。
>
> **这次参照图的可信度比第三/四提案高一截，值得说一句**：第三/四提案的色值都是 Remy 目测报的数字，或者转达的文字描述，我没法逐值核对。这次 Remy 直接给的是配色卡上标注好的精确 hex（`#494f2f`/`#9b9856`/`#e1e2dc`/`#e0dac2`），不是估的，下面所有推导都从这四个精确值出发，不存在"目测误差"这层不确定性。

## 0. 方法论先说一句，免得每条判断都要重复解释一遍

下面所有新增/调整的色号，用的是同一条方法：**色相和饱和度尽量直接沿用参照色板本身的四个值，只在明度这一个维度上做局部微调，来满足对比度或者视觉层级需要，不额外发明新色相**。唯一的例外是"该收"这个财务语义色，例外的原因见 3.4 节，会单独交代清楚为什么这次必须跳出参照色板本身的色相。

## 1. 先看懂这四个颜色：两组天然配对的色相家族，不是四个孤立的颜色

把四个颜色换算成 HSL：

| 颜色 | Hex | HSL |
|---|---|---|
| Wild Thyme | `#494f2f` | H71° / S25% / L25% |
| Lemon Sage | `#9b9856` | H57° / S29% / L47% |
| Porcelain Mist | `#e1e2dc` | H70° / S9% / L88% |
| Desert Linen | `#e0dac2` | H48° / S33% / L82% |

算完能看出这四个颜色其实分成两组，各自色相几乎重合：

- **A 组（H70-71°，橄榄绿）**：Wild Thyme（暗/浓）+ Porcelain Mist（极浅/淡），色相只差 1°，天生就是"同一个绿，一深一浅"——这组适合拿来做"背景 + 主强调色"这一对，深浅两端直接对应"Hero 实底"和"页面底色"。
- **B 组（H48-57°，暖金黄绿）**：Lemon Sage（中）+ Desert Linen（浅），色相只差 9°，天生适合做"次要强调色/装饰色"这一组，正好接手现有系统里 `gold`/`gold-dk`/`gold-lt` 这个角色（现有 gold 家族本来就落在暖黄棕这个区间，方向一致，不用整个推翻）。

这个结构比"四个颜色各挑一个用途"更省事：背景和主色本来就同源，不用另外去凑对比度层级；`gold` 系也不用凭空造一个新色相，直接把它锚到 B 组上即可。下面所有判断都是按这个结构往下派生。

## 2. 完整 token 清单表（这次最关键的交付，背景/卡片/主色 + 每一个文字色 token 全覆盖）

**表里"新值"一栏，只要不特别注明"零成本选项"，都是我推荐的默认落地值；对比度栏是配对的实际背景算出来的实测值，不是笼统数字。**

| 分类 | Token / Class | 用量 | 旧值 | 新值 | 配对背景 & 对比度 | 判断依据（简） |
|---|---|---|---|---|---|---|
| 背景 | `paper` | 全站底色 | `#FEFCF7`（第三/四提案曾定 `#F5EFDD`，从未落地代码） | **`#E1DFD3`** | — | Porcelain Mist : Desert Linen ≈ 65:35 混出来的，H51°/S19%/L86%，比纯 Porcelain Mist 多一点暖度，比纯 Desert Linen 淡，见 3.1 |
| 卡片底色 | `bg-white` | 全站卡片 | `#FFFFFF` | 不变 `#FFFFFF` | 卡片对新 paper 亮度差 14pt | 白卡对暖灰绿底的"抬升感"依然成立，不用改 |
| 卡片边框 | `sand`（token 名不变，值换） | ~10 处 `border-sand` | `#EDE8DA` → 三提案曾改 `#D9E3ED` | **`#BCC2A3`** | 装饰用，无文字对比度要求 | Wild Thyme 色相(H71°)衍生的浅灰绿，S20%/L70%，跟主色同源但够浅不抢戏 |
| Hero/CTA/选中态主色 | `ink` / `accent-700` | 遍布全站标题+按钮+选中态+Hero 底 | `ink #23232E` / `accent-700 #1A1A2E` | **`#494F2F`**（两个 token 这次统一成同一个值） | — | 直接采用 Wild Thyme 原值，不用再调，见 3.2 |
| 按钮按下态 | `accent-800` | FAB/按钮 hover | `#121122` | **`#363922`** | — | Wild Thyme 加深一档（L25%→18%） |
| 预留浅色阶 | `accent-600`（代码未实际引用） | 0 | `#2b2b45` | `#687043` | — | Wild Thyme 提亮到 L35%，仅为保持色阶完整，不影响任何画面 |
| 预留浅色阶 | `accent-100`（代码未实际引用） | 0 | `#c9c5d6` | `#E6E8D9` | — | 同上，Wild Thyme L88% |
| 预留浅色阶 | `accent-50`（代码未实际引用） | 0 | `#eceaf1` | `#F4F5EF` | — | 同上，Wild Thyme L95% |
| Hero 渐变 | `hero-gradient` 三段 | Hero 卡背景 | `#2A2A38`/`#23232E`/`#1B1B24` | `#565C3A`/`#494F2F`/`#363922` | — | 同一色相三段式分层，做法照抄第六版逻辑，只换锚点色 |
| Hero/卡片阴影锚点 | `shadow-hero`/`shadow-card` rgba | 全站 | rgba(20,20,30,…) / rgba(35,35,46,…) | **rgba(73,79,47,…)**（Wild Thyme 的 RGB） | — | 阴影颜色锚定新主色 RGB，跟第七版"锚定 ink RGB 不借用中性灰"同一条纪律 |
| 文字·标题/主文字 | `text-ink` | 33 处 | `#23232E` | **`#494F2F`**（跟上面 Hero 主色同一个值） | 对新 paper 6.43:1 | 直接复用主色，不用另开一个"标题色"token |
| 文字·次要说明/caption | `text-muted` | 63 处，全站最高频 | `#8A7A6A` | **`#685C50`** | 对新 paper **4.85:1**（旧值对旧 paper 已经只有 4.04:1，对新 paper 会掉到 3.18:1，不达标）| 保留原 H30°/S13% 不动，只把 L 从 47.8% 压到 36% 补回对比度，见 3.5 |
| 文字·错误提示 | `text-coral` | 19 处 | `#E8554E` | **`#991B14`** | 对新 paper **6.23:1**（旧值对旧 paper 只有 3.51:1，对新 paper 只剩 2.76:1，一直没达标）| 保留原 H3°/S77% 不动，只把 L 从 61% 压到 34%，见 3.5 |
| 文字·行程标题/待定态 | `text-gold-dk` | 8 处 | `#7E6630` | **`#5C5A33`** | 对新 paper **5.29:1** | 是不是该跟 Lemon Sage 合并的关键判断，见 3.3，结论：合并色相但不能字面等值 |
| 文字·Hero 深底主文字 | `text-white` | 5 处 | `#FFFFFF` | 不变 | 对新 Hero 主色 **8.60:1** | 旧 ink 约 9:1，新主色略降但仍然很充裕，不用改 |
| 文字·Hero"我的净额"标签 | `text-slate-400`（需替换成自定义 token，建议名 `hero-label`） | 1 处 | Tailwind slate-400（冷灰） | **`#BDC0AF`** | 对新 Hero 主色 **4.64:1** | 冷灰放在暖橄榄绿 Hero 上会很突兀，改用 Porcelain Mist 家族的浅灰绿，见 3.6 |
| 文字·Hero"邀请码"徽章 | `text-slate-200` | 1 处 | Tailwind slate-200（冷灰） | **复用 `text-paper`（即 `#E1DFD3`）** | 对新 Hero 主色 **6.60:1** | 同上，直接借新 paper 值，不用另开 token |
| 文字·该付/警示（浅底） | `text-red-600` | 4 处 | `#DC2626` | **`#A63926`**（建议新增 token `negative`） | 对新 paper **4.85:1** | 见 3.4，terracotta 系，跟 `coral` 是两个不同语义、刻意分开 |
| 文字·该付/警示（Hero 深底） | `text-red-400` | 1 处 | Tailwind red-400 | **`#E6B1A8`**（建议新增 token `negative-dk`） | 对新 Hero 主色 **4.60:1** | 同上系列的浅色变体 |
| 文字·该收/正向（浅底） | `text-emerald-600`/`text-emerald-700` | 5 处（含 expense-form 校验通过态） | `#059669` | 选项 A 零成本：不变／**选项 B（推荐）`#3E4374`**（建议新增 token `positive`） | 选项 B 对新 paper **6.95:1** | 见 3.4，这次不能延用第三提案的橄榄绿建议，改用刻意跳出色相的靛蓝 |
| 文字·该收/正向（Hero 深底） | `text-emerald-400` | 1 处 | Tailwind emerald-400 | 选项 A 零成本：不变／**选项 B `#BABDDE`**（`positive-dk`） | 选项 B 对新 Hero 主色 **4.68:1** | 同上系列的浅色变体 |
| 文字·完成态徽章 | `text-ok`（配 `ok-bg` 自成一体） | 3 处 | `#4C7A50` | 不变 | 自带底色，不直接对 paper | 范围外，且跟主色 H 差 54°，混淆风险低，见 3.6 |
| 文字·进行中态徽章 | `text-live`（配 `live-bg` 自成一体） | 1 处 | `#3E7787` | 不变 | 自带底色，不直接对 paper | 范围外；但如果"该收"选靛蓝方案，已刻意跟它拉开 42° 色相差，见 3.6 |
| 装饰色·预留（代码未实际引用为文字/背景 class） | `gold` | 0 | `#B89E61` | **`#9B9856`**（Lemon Sage 原值，无对比度约束） | — | 见 3.3 |
| 背景·徽章配对 | `gold-lt` | 2 处 `bg-gold-lt` | `#F0E8D6` | 不变 | — | 跟 Desert Linen（H48°）只差 6°，本来就在同一家族，不用改，见 3.3 |
| 语义色·比价最优 | `seafoam`/`sf-lt` | 2 处 | `#2DAA85`/`#D0F0E5` | 不变（可选微调 `#3D8F76`，非必须） | — | 范围外，评估结论见 3.6 |

## 3. 关键判断逐条展开

### 3.1 paper 背景：为什么不直接用参照图任一原值，而是混出一个新值

Porcelain Mist（`#e1e2dc`，H70°/S9%/L88%）单独拿来做整页背景会偏灰、偏冷，虽然色相跟 Wild Thyme 同源，但饱和度只有 9%，大面积铺开容易读成"中性灰"而不是"暖色系的一部分"，跟这个项目从第三提案起就在强调的"温暖个人记账本"这个定位有点脱节。Desert Linen（`#e0dac2`，H48°/S33%/L82%）单独用又太黄太重，饱和度 33% 对一整页背景来说偏高，会跟卡片、文字抢注意力。

按 65:35（Porcelain Mist 为主，掺一点 Desert Linen 提暖）混出 `#E1DFD3`（H51°/S19%/L86%），色相正好落在 A 组（70-71°）和 B 组（48-57°）中间，相当于把两组家族在背景这一层先做了个视觉上的"桥接"，后面不管是主色（A 组）还是 gold 系（B 组）叠上去都不会显得突兀。这不是凭感觉配的比例，是先看过两个纯色单独铺开都不够理想，再往中间找。

### 3.2 Hero/CTA 主色：为什么直接用 Wild Thyme 原值，不用再调

`#494F2F` = H71°/S25%/L25%。跟第四提案"藏青要多亮才够"那次遇到的问题不一样——藏青那次的 `#1E4B78` 需要调亮是因为原始饱和度太低（S14%）显得发黑；Wild Thyme 本身饱和度就有 25%，明度 25% 虽然深，但橄榄绿这个色相在低明度下依然能读出"绿"而不会像藏青那样被吃成灰黑（绿色相在人眼感知上天然比蓝紫更容易在暗部保留辨识度）。配白字对比度 8.60:1，充裕。这次不需要重复第四提案那套"提亮多少才够"的推导，Remy 给的原值直接能用。

### 3.3 gold 系要不要跟 Lemon Sage 合并——这次风险最高的一条，算给 Remy 看

现有 `gold-dk #7E6630` = H42°/S45%/L34%，Lemon Sage `#9b9856` = H57°/S29%/L47%。色相差 15°、饱和度差 16pt、明度差 13pt——不是完全撞在一起，但落在同一个"暖黄棕/橄榄"大区间里，是这份简报反复栽过跟头的那种"两个颜色太像被当成两个不同变量"的坑（`gold` vs brand-pm `accent`、`gold-dk` vs brand-pm `wait` 都是前车之鉴），这次风险更高，因为连色相家族都撞上了。

**能不能字面上直接把 `gold-dk` 换成 Lemon Sage 原值？算了一下，不能。** Lemon Sage 原值 `#9b9856` 配白/paper 底文本对比度只有约 3:1，`text-gold-dk` 目前是 8-12.5px 的小字（行程标题、徽章文字），按 WCAG 正文标准需要 4.5:1，直接换会导致文字看不清，这是个硬约束，不是风格取舍。

**正确的合并方式是"色相家族合并，数值不合并"**：把 `gold-dk` 保留 Lemon Sage 的 H57°/S29% 不变，只把明度从 47% 压到 28%（对新 paper 实测 5.29:1，达标），算出来是 `#5C5A33`。这样 `gold-dk` 在色轮上跟 Lemon Sage 是"同一族里深一档"的关系，不是两个各自为政的暖黄棕。同理，现在代码里其实完全没用到的 `gold`（只有 `gold-dk`/`gold-lt` 被实际引用），既然它本身没有文字对比度约束，直接给 Lemon Sage 原值 `#9B9856` 就行，不用再调。`gold-lt`（背景配对色）跟 Desert Linen 色相只差 6°，本来就在同一家族里，不用动。

**一个诚实的观察**：`gold-dk` 压到 L28% 之后，跟 Hero 主色 Wild Thyme（H71°/L25%）在明度上已经很接近（28% vs 25%，只差 3pt），色相也只差 14°——这是 Remy 给的参照色板本身自带的（Wild Thyme 和 Lemon Sage 在她的图里本来就是相邻色号，色相只差 14°），不是我这边操作出来的偏差。我判断这个问题不大，因为两者出现的语境不一样：`gold-dk` 是行程标题这种小字号文字，Wild Thyme 是 Hero 大色块，尺寸和场景的差异本身就能防止混淆；如果实机看起来还是分不清，退路是把 `gold-dk` 的饱和度往上提一档（比如提到 40%）靠色彩浓度而不是色相去拉开差距，不用改色相。

### 3.4 该收/该付：为什么这次不能照搬第三提案的橄榄绿建议

第三提案给"该收"推荐过暖调橄榄绿 `#4D7D36`（H101°/S40%/L35%），当时的逻辑是配藏青背景时橄榄绿更协调。这次背景/主色本身已经是橄榄绿（Wild Thyme H71°），如果"该收"继续用 `#4D7D36` 这种橄榄绿，色相只差 30°，两个都是中低饱和度的暗绿，会直接撞进"该收看起来跟 Hero 主色是同一个颜色"这个坑——这正是任务里点名要重新判断的地方，第三提案那条建议是针对不同背景想的，不能直接搬过来用。

同理，继续沿用 Tailwind 默认 `emerald-600`（H161°/S94%/L30%）反而问题不大：色相跟 Wild Thyme 差 90°，而且饱和度高出快 4 倍（94% vs 25%），一个是鲜艳的青绿、一个是发闷的橄榄绿，实际视觉上不会读成同一类颜色。但它本身是 Tailwind 完全没调过的默认色，跟"绝对禁止清单"点名过的"配色是没人挑过的默认工具色"是同一类问题，风格上跟这套已经开始有性格的自然调色板放在一起会显得突兀（这条是 3rd 提案就点出来过的老问题，没变过）。

**我的判断：这次"该收"应该刻意跳出参照色板的色相，用一个冷色系。** 理由不是随便选的：这个палette 里能用的暖色区间已经被 Wild Thyme（橄榄绿主色）、Lemon Sage/gold 系（装饰色）、terracotta（该付警示色，见下段）占满了，财务语义色需要的是"一眼能跟其它东西分开"，硬要在暖色系里再挤一个"该收"色，不管挑哪个都容易被读成"这也是橄榄绿家族"。选 `#3E4374`（H235°/S30%/L35%，暗denim靛蓝），对新 paper 对比度 6.95:1，配 Hero 深底的浅色变体 `#BABDDE` 对比度 4.68:1。选这个色相还有一个附加考量：现有 `live`（进行中态，`#3E7787`，H193°）也是个冷蓝绿，跟我新选的靛蓝差了 42° 色相，特意留出这个距离是为了不让"该收"跟"邀请链接有效"这两个不相关的语义在视觉上撞在一起。

**这条给两个选项，跟第三提案一样的结构**：选项 A 零成本维持 `emerald-600` 不变（风险可控但风格上略突兀）；选项 B（我的推荐）换成靛蓝 `#3E4374`/`#BABDDE`，需要改约 5 处代码（新增 `positive`/`positive-dk` token，替换 `text-emerald-600`/`text-emerald-400`/`text-emerald-700` 三类引用），改动量不大，交给 Remy 最终定。

**该付这次判断更简单，维持第三/四提案定案的思路（terracotta 系），只是要重新压暗一档**：现有第三提案定的 `#C1432C`（H9°/S63%/L46.5%）配旧 paper（接近纯白）对比度约 5:1，能过；但配这次更暗的新 paper（`#E1DFD3`，L86%）实测只剩 3.93:1，掉到不达标。保留原 H9°/S63% 不变，把 L 从 46.5% 压到 40%，算出来 `#A63926`，对新 paper 对比度回到 4.85:1。Hero 深底的浅色变体 `#E6B1A8` 对比度 4.60:1。

**一个需要留意但不算严重的风险**：压暗后的 `coral`（`#991B14`，H3°/S77%/L34%，错误提示专用）跟压暗后的"该付" terracotta（`#A63926`，H9°/S63%/L40%）色相只差 6°，比原来两版都更接近了。`coral` 和"该付"从第三提案起就被"绝对禁止第 12 条"要求刻意分开（一个是表单校验报错，一个是财务欠款方向），我判断目前还能分清（`coral` 饱和度更高更艳、更暗，`该付` 更偏土红/锈色），但如果 Remy 实机看着还是分不太清，退路是把 `coral` 的色相往纯红/品红方向再挪一点（比如 H350° 附近），跟 terracotta 拉开更明显的距离。

### 3.5 顺手修掉的两处既有对比度问题：不是这次改色带出来的新 bug，是本来就该修

算 `text-muted`/`text-coral` 对新背景的对比度时发现，这两个颜色**在旧背景（`paper #FEFCF7`，接近纯白）下就已经不达标**：`muted` 旧值对旧 paper 实测约 4.04:1（WCAG AA 门槛 4.5:1），`coral` 旧值对旧 paper 实测约 3.51:1，两个都是本来就踩线失败的，只是旧背景够亮，肉眼不容易察觉。这次背景换成明度更低的新 paper（`E1DFD3`，L86% vs 旧 paper L97%）之后，两个都会进一步跌到 3.18:1 / 2.76:1，问题被放大到肉眼也能感觉出来的程度。这次一并修：`muted` 保留原色相不变只压暗明度（`#8A7A6A`→`#685C50`），`coral` 同理（`#E8554E`→`#991B14`），两个都恢复到 4.5:1 以上，顺带把这个历史遗留问题解决掉，不算这次改色范围外的事。

### 3.6 seafoam / ok / live：范围外，但既然任务点名要求评估，给出结论

- **seafoam（`#2DAA85`，H162°/S58%/L42%）**：跟 Wild Thyme（H71°）色相差 91°，一个是鲜艳青绿、一个是发闷橄榄绿，混淆风险低，不用改。唯一值得一提的是它的饱和度（58%）比这套新调色板里其它任何颜色都高（Wild Thyme 25%、Lemon Sage 29%、Desert Linen 33%），摆在一起会显得比周围"更艳/更合成"，风格上略突兀但不是错误。如果要精修，可选降饱和版本 `#3D8F76`（H162°/S40%/L40%），不是必须。
- **ok（`#4C7A50`，H125°/S23%/L39%）**：跟 Wild Thyme 色相差 54°，且总是配着自己的 `ok-bg` 底色一起出现（独立徽章，不直接铺在 paper 上），混淆风险低，维持不变。
- **live（`#3E7787`，H193°/S37%/L39%）**：同上，配 `live-bg` 自成一体。这次新增的"该收"靛蓝候选（H235°）跟它已经刻意隔开 42° 色相，避免"该收"和"邀请链接有效"这两个不相关的状态在视觉上撞车。

## 4. 字体：自然草木调下，Fraunces 还合适吗

**能力上依然没问题，但用法上需要收着点用，不建议用它最"闹"的那一端。** Fraunces 是暖调怪奇衬线体，前几版"复古海边度假明信片"调性下，它的宽笔画、怪奇曲线正好呼应"旅行明信片"那种带点俏皮感的复古气质。这次参照图的气质不一样，是"自然/植物学/天然材质"——想象高级天然护肤品或草本茶包装那种调性，通常偏向克制、优雅、不张扬的排版，跟"明信片的俏皮"是两种不同的性格。Fraunces 如果用最重字重（700）配斜体，会显得比这个新调性想要的更"闹"、更有戏剧性。

**结论：继续用 Fraunces，不引入新字体，但用法上收窄到偏克制的一端**——净额数字这类大字号展示场景，优先用中等字重（500-600）、正常体（不用斜体），避免用最重的 700+斜体这种组合。这个判断不是钉死的规则，是给 frontend-dev 一个默认倾向，实机看效果如果觉得还是需要更饱满的字重表达"净额"这种重要数字，可以按具体场景微调，不算破坏这条纪律。

**中文标题字体分工不变**：Fraunces 依然严格限定在阿拉伯数字金额场景，不碰中文（绝对禁止第 6/14 条），这条纪律这次不重新论证第二次，直接继承。

## 5. 这次没有改动的东西

- 紧凑尺寸基准（`user_compact_ui_preference.md`）——这次也是纯色彩提案，字号/padding/触控热区一个数字都不碰，Hero 从第四提案定的"满宽色块，只加宽外框不放大内部元素"这条纪律继续有效
- 圆角尺度、emoji 默认图标语言、Plex Mono 使用边界——不重新讨论
- 装饰元素（邮票齿边卡片/贝壳太阳纹线稿图标）——第三提案定的"加分项，非必需"结论继续有效，这份提案没有新的参照图涉及这块，不重新展开
- Fraunces 只用数字、Inter 管中文——不变

这份提案只出方向，实现前照惯例先出一版静态视觉样例（新背景 + Wild Thyme 主色 + 满宽 Hero 色块 + 靛蓝"该收"标签，一起截一张实机模拟图）让 Remy 确认色板方向没走偏，再动真实代码。

---

# 第六提案：候选 C「ON ICE」与候选 D「渐变灰阶」角色分配方案（2026-09-11，第十七轮）

> **这次的性质**：候选 A（Wild Thyme，上面第五提案）已经拍板退场，候选 B（暖阳蓝调，333 × Sunny Stripes 融合）原样保留当基准，本身的角色分配写在 `DESIGN-BRIEF-color-v6-preview.html` 自己的色板速查区块里，这份文件不重复整理。这次 Remy 直接给了候选 C、候选 D 两组新色板，要求各自重新设计角色分工（不能把颜色硬套进候选 B 现成的 token 角色），跟第三/四/五提案那种"猜方向"性质不同，这次色号是钉死的，只需要判断"每个色号该配到哪个角色、为什么"。方案先用文字回给 Remy 确认三个分歧点，确认后才建 12 屏真机预览，实现结果落在 `DESIGN-BRIEF-color-v6-preview.html`。

## 候选 C「ON ICE」（Remy 指定色板：巧克力深棕 `#4e3324` / 浅牛仔蓝 `#9ec3d6` / 深牛仔蓝 `#6e8fa3` / 暖米色 `#e6d6c6` / 近白 `#f2f2ee` / taupe 灰褐 `#9c8b7a`）

整体故事是「冰」和「巧克力」两极：巧克力做全站最深的颜色，两档牛仔蓝做「冰」那半边的强调色，taupe 单独留给边框（经典中性灰褐，划边界不跟冰的蓝、巧克力的棕抢戏）。

| Token | 角色 | 取值 | 为什么 |
|---|---|---|---|
| `--a-paper` | 全站背景 | `#f2f2ee` 近白 | 6 色里最浅，直接当页面底色 |
| `--a-ink` | 标题/Hero/CTA/选中态 | `#4e3324` 巧克力深棕 | 全站最深的颜色，撑标题对比度 |
| `--a-accent-800` | Hero渐变最深端 | `color-mix(#4e3324 78%, black)` | 在巧克力基础上再压暗，可追溯回同一色源，不另造新色号 |
| `--a-muted` | 次要说明文字 | `#9c8b7a` taupe（满饱和度） | taupe 本身饱和度低、调性就是"次要"，跟 sand/field-border 同源但保持满饱和区分层次 |
| `--a-sand` | 卡片/tx-item 边框 | `color-mix(#9c8b7a 32%, white)` | taupe 稀释到 32%，做边框够淡不抢戏 |
| `--a-field-border` | 输入框边框 | `color-mix(#9c8b7a 16%, white)` | taupe 稀释到 16%，比 sand 更淡一档（延续第六轮"输入框边框更淡"规矩） |
| `--a-card-tint` | 卡片浅底 | `rgba(230,214,198,.4)`（暖米转半透明） | 跟 sand 同源都是暖灰调，只是透明度不同，摸起来像"巧克力牛奶"的柔和感 |
| `--a-gold-dk` | 第二识别色文字/待定态徽章/quickadd-select文字 | `#6e8fa3` 深牛仔蓝 | gold 系这版不是真金色，改当候选C自己的第二识别色 |
| `--a-gold-lt` | 徽章浅底 | `color-mix(#9ec3d6 32%, white)` | 浅牛仔蓝稀释到三成 |
| `--a-gold` | 装饰（暂未实际引用） | `#9ec3d6` 浅牛仔蓝原值 | 备用强调色，不引入色板之外的新颜色 |
| `--a-hero-label` | Hero深底标签文字 | `color-mix(#9ec3d6 65%, white)` | 浅牛仔蓝稀释更淡，巧克力深底上足够亮好读，延续"冰"的色相 |
| `--a-ice`（新增） | 头像圈/图标圈/未选中分段按钮描边 | `#9ec3d6` 浅牛仔蓝原值 | 圆形/线框元素专用的"冰"色 |

**Remy 确认结果（2026-09-11）**：维持这版分配（边框用taupe、浅底用暖米），不改用"暖米做边框、牛仔蓝做浅底"的备选逻辑。

## 候选 D「渐变灰阶」（Remy 指定色板：Bleached Silk `#F7F7F6` / First Star `#DBDAD6` / Mountain Mist `#A4A3A0` / Welded Iron `#6E6E6C` / Black Olive `#373736`，5 阶由浅到深）

核心逻辑是「深浅当色相用」——没有第二个色相可以拿来区分背景/文字/强调/第二识别色，全靠 5 级灰阶的明暗顺序分层。5 色比候选 C 少一个，`--a-muted` 和 `--a-gold-dk` 刻意共用同一个色号（都算"次于 ink 的深灰"）。

| Token | 角色 | 取值 | 为什么 |
|---|---|---|---|
| `--a-paper` | 全站背景 | `#F7F7F6` Bleached Silk | 5阶最浅 |
| `--a-ink` | 标题/Hero/CTA/选中态 | `#373736` Black Olive | 5阶最深，唯一够深撑住正文标题对比度，也是"渐变灰阶"名字里最深那一端 |
| `--a-accent-800` | Hero渐变最深端 | `color-mix(#373736 82%, black)` | 在Black Olive基础上再压暗，不另造新色号 |
| `--a-muted` | 次要文字 | `#6E6E6C` Welded Iron | 比ink浅一档，"次要文字比标题浅一档"是经典搭配逻辑 |
| `--a-gold-dk` | 第二识别色（第二行程dot/待定态文字） | `#6E6E6C` Welded Iron（跟muted共用） | 灰阶没有第二色相可用，只能靠深浅区分"第一/第二"，两个角色语义上都算"次于ink的深灰"，算同一类东西 |
| `--a-sand` | 卡片/tx-item边框 | `#DBDAD6` First Star | 5阶第二浅 |
| `--a-field-border` | 输入框边框 | `color-mix(#DBDAD6 55%, white)` | First Star稀释55%，比sand更淡一档 |
| `--a-card-tint` | 卡片浅底 | `rgba(164,163,160,.14)`（Mountain Mist转极浅透明） | 卡片比纯白背景多一点点灰调质感，但很克制 |
| `--a-gold-lt` | 徽章浅底 | `color-mix(#A4A3A0 30%, white)` | Mountain Mist稀释三成 |
| `--a-gold` | 装饰（暂未实际引用） | `#A4A3A0` Mountain Mist原值 | 备用强调色 |
| `--a-hero-label` | Hero深底标签文字 | `color-mix(#DBDAD6 70%, white)` | First Star稀释更淡，深灰底上足够亮好读 |
| `--a-accent-circle`（新增） | 头像圈/图标圈/未选中分段按钮描边 | `#A4A3A0` Mountain Mist原值 | 比gold-lt徽章浅底再深一档，让"点"和"字"在灰阶里各占一级；没有再造第6个灰阶，是刻意的——多造一级会破坏"就是这5阶灰"的干净感 |

**Remy 确认结果（2026-09-11）**：维持纯灰阶方向①，强调色（CTA/头像圈等）不额外引入彩色，让财务语义色（该收该付/完成态）成为全页面唯一的彩色。

## 两个候选共用的部分（这版没有重新设计）

- 语义色（该收该付/完成态/进行中态/比价最优）：三个候选（B/C/D）完全不动，跟候选 A/B 共用同一份定义，见「共用语义色板速查」区块。遇到浅色语义字叠在候选自己深底上看不清的情况，靠调整各自 Hero 渐变的深浅排布位置解决，没有改语义色 token 本身的值。
- Hero 呈现方式（满宽通栏色块、方形直角、无装饰图标）：沿用第四提案定案的做法，这轮没有重新讨论。

## 首页「总消费」汇总显示格式（附带一并确认的点，非配色但同批表态）

三个候选：①只显示本位币总额（已实现）②同原始币种才显示双值,混合退回① ③严格列出每种原始币种小计。**Remy 确认结果（2026-09-11）：选①**，维持已实现的版本，理由是最简洁也最不容易在紧凑排版基准上翻车。

## 后续状态

三点确认后，lifeos-pm 派独立 agent 对候选 C/D 的完整 12 屏做了核实（grep 精确数过屏数，非估算）+ ui-auditor 审美走查，结论"可以过审"，附两条非阻断观察点：候选C的"冰"元素只集中在头像圈/图标圈这类小圆形元素上，首页/记账/结算等高频屏基本看不到蓝色，跟"冰与巧克力两极对比"的命名预期可能有落差；候选D卡片和页面背景色太接近，层次感主要靠1px描边撑着，这是纯灰阶方案的必然代价，非做错。这两条留给 Remy 自己肉眼确认要不要接受，不影响本轮交付判定。
