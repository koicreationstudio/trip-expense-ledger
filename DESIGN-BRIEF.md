# trip-expense-ledger 设计简报

> 首份设计简报（Step 0 判断：这是全新项目，之前从没认真设计过一版视觉，直接从 v0.1 功能实装阶段的裸 Tailwind 状态开始定调）。

## Step 0：先说清楚现状是什么，给谁看，在什么设备上看

读了 `tailwind.config.ts`（`theme.extend` 是空的，从没自定义过任何东西）、`app/globals.css`（只写了 `body { background-color: #f8fafc; color: #0f172a }`，这两个色值就是 Tailwind 默认 slate-50 / slate-900 原封不动）、`app/layout.tsx`（没有任何 `next/font` 引入，字体走的是浏览器默认无衬线体）、首页/行程主页/记消费/结算这几个核心页面（52 处 `rounded-md`，按钮清一色 `bg-slate-900`，列表清一色套同一个白底灰边框盒子），以及 `package.json`（没装任何图标库，全项目 grep 不到一个 emoji）。

结论：**Remy 说"一点设计感都没有"是准的，但准确定性不是"AI 默认脸"那种蓝紫渐变、圆角卡片堆砌、emoji 图标的翻车（这几样代码里一个都没有），而是另一种更朴素的翻车——从头到尾没有做过任何视觉决定**。颜色是 Tailwind 文档里抄来的默认值，字体是浏览器兜底值，没有强调色，没有图标，所有区块都套用同一个毫无层级的白盒子。说白了就是"功能做完了，设计这一步还没轮到"，不是"设计错了"，是"设计还没发生"。

**给谁看**：读 `README.md` 能看到这是要发布到公开 GitHub 的开源自托管项目（`git clone github.com/<你的用户名>/trip-expense-ledger`），日常真正用的人是 Remy 和一起出差的朋友，但**决定要不要自托管这个工具的人，是先去 GitHub 看 README 和截图的陌生开发者**。这两拨受众都要照顾到：给 Remy 自己用不需要花哨，但要在 GitHub 截图里让人一眼觉得"这是个认真做的工具"，不是"这是个功能能跑的练习项目"。所以调性判断上要偏向"给外人看的展示物"多一点，不能只按纯内部工具的朴素标准来。

**⭐在什么设备上看（关键约束，优先级最高）**：这个 App 的真实使用场景绝大多数是**手机**——出差路上刚付完钱，掏出手机马上记一笔；查同行人代垫了多少、该转多少钱，也是在手机上边走边看，不是坐在电脑前打开浏览器慢慢看仪表盘。所以**所有定调性、参照对象、字号/留白/触控目标大小的判断都要以手机单手操作为第一优先级，桌面浏览器是"顺便能用"的次要适配，不是反过来先设计桌面版再缩小成手机版**。

现在代码里全项目只有 1 处响应式断点类（`sm:`），等于从没认真做过手机/桌面的区分设计；容器是 `mx-auto max-w-3xl px-4 py-8`，在手机宽度下就是全宽 + 16px 左右边距，这个基础布局思路（不做多栏，单栏往下排）是对的方向，但字号（大量 `text-sm`/`text-xs`，很多正文/说明文字只有 12-14px）和按钮点击区域（`px-4 py-2 text-sm` 算下来实际点击高度约 36px）都明显是按"桌面网页"的手感调的，不是按"户外阳光下、走路时单手点、可能戴着行李箱手套"的手机手感调的。这条要在下面的视觉决定里具体改。

## 定位

**一句话**：打开它的第一印象应该是"这是一个把钱算得很准、在手机上一只手就能用顺的专业记账工具"，不是"这是一个刚学会 Next.js 的桌面网页练习项目"。

**调性关键词**：
- **手机优先，单手可操作**——高频动作（记一笔消费）要在拇指自然能点到的地方，字号/点击区域按手机手感调，不是缩小版的桌面网页
- **克制精确**——数字对齐、层级清楚，不是可爱活泼的消费类 App
- **一个态度鲜明的强调色，其余全走灰阶**——不是随手抄来的默认蓝，也不是到处撒彩色的花哨界面
- **单列信息流，紧凑但不拥挤**——手机屏幕窄，不能照搬桌面 dashboard 的多列表格，但也不能因为"克制"就把留白堆得太多导致一屏看不到几行有用信息
- **工具感而非消费品感**——这是记账工具不是社交 App，别往可爱/游戏化方向靠

## 参照

**1. Linear**——参照它"整个产品界面只留一个强调色，其余全部走灰阶"这条纪律。Linear 的画布是近黑 `#08090a`，唯一的彩色是靛蓝 `#5e6ad2` / `#7170ff`，边框和层级全靠白色透明度的深浅变化做出来，不是靠加更多颜色。字体是 Inter Variable，常用字重从 300 一路用到 590，靠字重（不是靠加颜色/加图标）建立标题和正文的层级。这个项目不需要抄它的黑底暗色（本项目走 light mode），但"整站一个强调色、其余灰阶、层级靠字重不靠花哨装饰"这条纪律要照抄。

**2. Wise（尤其是它的手机 App，不只是官网）**——参照两件事：一是它"钱的产品不必然长得像银行安全蓝"这个态度，2026 年那次品牌重塑从"安全蓝"换成亮青柠绿 `#9FE870` + 森林绿 `#163300`，就是为了在一片"看起来都很专业但都长一个样"的金融产品里做出记忆点；二是 Wise 作为一个日常在手机上边走边用的转账/换汇工具，核心操作（转账、查汇率）都做成大触控目标、拇指区域内可达、不需要精细点击。这个项目的差异化卖点（汇率比对 + 算哪张卡最划算）本来就是 Wise 的核心功能在做的事，参照的不是它具体那个绿色（照搬颜色容易被人以为山寨 Wise），而是它选强调色的逻辑，以及它作为手机端理财工具对"点得到、点得准"的重视。

**3. Stripe Dashboard**——参照它处理金额数字的具体做法：财务数据一律用等宽数字（`tabular-nums`）、右对齐，跟正文数字分开处理；表格本身信息排得紧凑，但表格四周的留白给得宽松，让"数据紧凑"和"呼吸感"分层而不是混在一起显得又挤又乱。这个项目的消费列表、结算净值表、转账清单，全是"一堆金额数字要对齐好读"的场景，直接照抄这个具体技法就够——只是 Stripe 这套是桌面宽屏场景想出来的，搬过来时表格要收成单列卡片（金额固定靠右对齐），不能真的在手机屏幕上塞一个多列表格。

（没有专门找"手机记账 App"当第 4 个参照，是因为上面三条已经把"整体克制纪律""强调色选取逻辑""金额数字排版技法"三件事讲清楚了；手机端的具体尺寸/触控标准直接用下面视觉决定里列的 Apple/Material 官方数值就够，不需要再找一个产品当参照凑数。）

## 绝对禁止

以下每一条都是**这个项目接下来最可能踩的坑**，不是套话清单：

1. **首页/登录页塞蓝紫渐变背景**——现在代码里没有这东西，但这是"AI 觉得页面太素就加渐变"的经典反射动作，尤其入口页最容易被顺手塞一个 `bg-gradient-to-r from-blue-500 to-purple-600`。这是记账工具不是营销落地页，加渐变只会显得不严肃，还会稀释掉本来该建立起来的强调色。

2. **emoji 当图标**（比如状态标 ✅❌，分类标 🍜🏨💰）——现在项目没装任何图标库，以后加状态/分类/导航图标时最容易图省事直接打 emoji。这是要挂在公开 GitHub 让陌生人自托管的开源工具，emoji 在 iOS / Windows / Linux 上渲染完全不一样，会把这个"帮你精确算钱"的工具拉低到"学生练习项目"的观感。真要加图标，用免费的图标库（比如 lucide-react，几何线条跟 Inter 字体气质搭），别用 emoji 顶替。

3. **继续用系统默认字体栈**——现在 `globals.css` 没有任何 `font-family` 声明，也没有接 `next/font`，渲染出来的字体就是浏览器兜底的无衬线体（Mac 上是 `-apple-system`，Windows 上是 `Segoe UI`）。这是 Remy 那句"一点设计感都没有"最直接的成因之一——字体从来没被人选择过，是浏览器随便给的默认值。必须显式挑一个字体接进来，不能再让浏览器兜底。

4. **顺手抄 Tailwind 文档默认色**（`bg-blue-600` 之类）——现在按钮统一是 `bg-slate-900`，整站没有一个"这是这个产品自己的颜色"的强调色。以后加主按钮/高亮状态最容易顺手抄 Tailwind 官方例子里的默认蓝。这个项目自己讲的差异化卖点就是理财类功能（比价选卡），理财类产品扎堆用"安全蓝"反而最没记忆点（Wise 改脸就是因为这个道理）。

5. **每加一个新区块都套同一个白盒子**（`rounded-md border border-slate-200 bg-white p-3`，现在 52 处 `rounded-md` 里绝大多数就是这个模板）——参与者列表、结算净值、转账清单三个区块现在长得一模一样，靠不出任何层级。以后加新内容不能继续无脑复制这个盒子，层级要靠字重/字号/留白的差异做出来，不是靠"再套一层卡片"。

6. **给每个按钮都加浮夸 hover 动效**（`hover:scale-105 hover:shadow-2xl` 这类）——现在完全没有交互动效，说明还在纯功能实现阶段，等做交互润色时最容易矫枉过正到处加放大/阴影动画让界面到处在跳。这是记账工具，操作要显得"稳、可信"，不是素材展示站那种炫技感；而且 hover 状态在手机触屏上本来就基本用不到（没有鼠标悬停这回事），花力气做 hover 动效是在给真正用不到它的桌面场景过度投入，挤占本该花在触控体验上的精力。

7. **按桌面网页的手感调字号/点击区域，权当"响应式"处理完了手机适配**——现在全项目只有 1 处 `sm:` 断点，121 处 `text-sm`（14px）、24 处 `text-xs`（12px），按钮实际点击高度约 36px。这是先有桌面思路、手机只是让它"能挤下"，不是从手机手感出发设计的。真正的手机优先是反过来：先按下面视觉决定里的触控标准把手机版调顺，桌面版是"同一套设计，多一点边距"，不是主战场。

## 视觉决定

- **色板**：保留 light mode（现有 `color-scheme: light` 不用改，手机在户外阳光下 light mode 通常比 dark mode 更好辨认）。中性灰阶继续用 slate（`slate-50` 背景 / `slate-900` 正文这套本身没错，不强制换成别的灰阶）。**新增一个强调色 `amber-700` (`#B45309`)**，只用在主按钮、关键链接、需要强调的操作，别的地方保持灰阶——照抄 Linear 那条"整站一个强调色，其余全灰"的纪律。财务语义色维持现状不用动：正值/该收 = `emerald-600`，负值/该付 = `red-600`，这是记账惯例不是"随手挑的绿红"，已经是对的。次要文字目前用 `text-slate-400`/`text-slate-500`，在手机户外强光下容易看不清，新增文案时优先用 `slate-500` 起步，避免再用更浅的灰。
  - ⚠️ 这个具体色号是我按"避开默认蓝、避开撞 Wise 的青柠绿、贴合出差/旅行的暖色联想（护照/登机牌纸质那种暖调）"这个逻辑推出来的判断，没有跟 Remy 核实过。只要保持"整站一个强调色 + 其余灰阶"这条纪律，颜色本身可以换。

- **字体**：标题和正文都用 **Inter**，通过 `next/font/google` 在构建时打包进产物、不在运行时连去 Google 服务器——这点比较关键，跟这个项目自己标榜的"数据自托管、不外流"的调性是一致的，不要用 `<link>` 标签引入外部字体 CDN。所有金额数字统一加 Tailwind 的 `tabular-nums`，让消费列表/结算页的数字对齐、好读。**只用 Inter 一种字体，靠字重区分层级就够**，不需要为了"看起来有设计"硬凑第二种展示字体。**手机端字号基准要上调**：正文至少 16px（`text-base`），现在大量用的 `text-sm`(14px)/`text-xs`(12px) 只留给真正次要的辅助说明（比如"本位币 MYR"这种元信息），不能是列表主要内容的默认字号；表单输入框字号也建议不低于 16px，避免 iOS Safari 因为输入框字号小于 16px 而自动放大整页面（常见移动端表单坑）。

- **圆角**：维持现在的小圆角（`rounded-md`，约 6px），不用放大到 `rounded-xl`/`rounded-2xl` 那种"友好气泡"风格。这是记账工具，小圆角/接近直角传达的"精确、正式"比大圆角"轻松可爱"更贴这个产品该有的调性——现有这个选择本身没错，继续沿用，不用改。

- **间距密度 + 触控目标（手机优先）**：单列信息流为主，不做桌面 dashboard 那种多列表格；金额一律放在每行最右侧、等宽数字对齐。**所有可点击元素（按钮/链接/列表项）最小点击热区不低于 44×44pt**（Apple Human Interface Guidelines 的官方最小触控标准，Android Material Design 对应是 48×48dp），现有 `px-4 py-2` 按钮（约 36px 高）要拉到至少 `py-3`（约 44-48px）。**高频操作要放在拇指容易够到的地方**：全项目最高频的动作是"记一笔消费"，不该是页面角落一个小链接，应该考虑做成固定在屏幕下方、单手持机时拇指自然落点的位置（比如底部常驻的主操作按钮），而不是像现在首页那样跟其他次要链接混排在一起。区块之间（section 与 section）留白保持现有 `gap-6`/`gap-8` 这个量级即可，这个不用因为"手机屏幕小"就压缩掉，压缩区块间距只会让整页看起来更挤，真正该压缩的是同一区块内部行与行的间距。

## 跟系统里其它项目的关系

**刻意不跟 Remy 系统内部项目的 design language 走**（比如 SUI 珠宝系统 / gem-deploy 那套暗色金色调）。原因：那些是给 Remy 自己和团队用的内部业务工具，受众固定、不需要对外说服任何人；这个项目是要发布到公开 GitHub、给陌生开发者自行判断要不要自托管的独立开源项目，受众和评估标准都不一样，不该被内部系统的视觉语言绑住。

目前 Remy 系统里也没有哪个项目是"公开 GitHub 开源工具"这个类型的先例，也没有哪个是"手机单手操作优先"这个使用场景的先例（内部系统大多是坐在电脑前用），所以这次没有内部参照可抄，调性判断和触控标准全部来自外部真实产品 + 官方移动端设计规范（见上面"参照"一节）。
