# trip-expense-ledger 设计简报（第二版，覆盖第一版）

> 第一版（commit 5c8bf55）已落地：Inter 字体、amber-700 单一强调色、44pt 触控热区、tabular-nums 金额对齐，ui-auditor 走查过视觉一致性和审美关都过了。但 Remy 反馈"一点都没有从使用者角度出发"，点名 Splitwise 和他自己做的 remy-thailand.pages.dev 视觉功能都更好。
>
> 诊断：**第一版参照对象选错了品类**。Linear / Wise / Stripe Dashboard 教会了这个项目"怎么克制地用颜色、怎么排数字"，但从没研究过"记账分摊"这个具体品类里市场验证过的**信息架构**——首屏该放什么、页面该长什么结构。第一版只做了视觉皮肤，没碰过信息架构。这一版要修的是这个层面，不是再调一次颜色字体。

## Step 0：这次是什么性质的改动

旧版视觉纪律（字体/强调色/触控热区/tabular-nums）依然成立，不重来。这次是**同一套视觉语言之下的信息架构大改**——页面里各区块的顺序、每个区块该显示什么数据、哪个操作该有多显眼，这些要重新想。

现状核实（这次真的重新读了当前代码，不是照抄调研文件）：

- `app/trips/[tripId]/page.tsx`（行程主页）：确认完全没有余额/欠款汇总区块。参与者列表只显示"已认领/未认领"文字状态，不显示金额。"我的消费记录"的查询条件是 `eq(expenses.enteredByParticipantId, identity.participantId)`——**只拉自己录入的消费，同行人互相看不到对方记了什么**，这在一个"同行人分摊"的工具里是结构性缺陷，不只是视觉问题。
- `app/trips/[tripId]/settlement/page.tsx`（结算页）：每人净值 + 转账清单的计算和数字排版（右对齐/tabular-nums/绿红配色）本身做得对，但整套逻辑锁在一个要点导航才能到的独立页面，不是打开行程第一眼看到的东西。
- `app/trips/[tripId]/expenses/expense-form.tsx`：比价功能（这个项目自己的差异化卖点）确实存在且逻辑能跑，"最划算"那一行现在只有 `bg-emerald-50` 底色 + `text-xs` 小字，视觉分量跟它的重要性不匹配。
- **订正一处调研文件里不准的判断**：调研文件说"全项目 grep 确认没有任何 fixed bottom 类的 FAB 实现"，这次重新读代码发现**不准确**——`app/trips/[tripId]/record-expense-fab.tsx` 已经存在且被 `layout.tsx` 引用，做的是一个 `fixed inset-x-0 bottom-0` 的**整宽度**常驻底部按钮条（在记消费/编辑页会自动隐藏，避免跟表单提交按钮打架，且处理了 `env(safe-area-inset-bottom)`）。机制是对的、工程细节也做了，但视觉呈现是"贴边通栏按钮条"而不是 remy-thailand 那种"浮在内容之上、右下角悬浮的圆形按钮"。这次的决定是**保留现有隐藏逻辑和安全区处理这两块工程实现，只改视觉呈现**（见下方"信息架构与页面结构"第 4 点），不是从零造一个 FAB。

给谁看、什么设备：延续第一版判断不变——手机单手操作优先（出差路上边走边记账），同时是要发布到公开 GitHub 给陌生开发者评估的开源项目，两头都要照顾。Apple HIG 44pt 触控热区标准延续第一版定案，这次不重新论证。

## 定位

**一句话**：打开它的第一印象应该是"这趟出差我到底该收该付多少钱，一眼看到"，不是"点进去找了三层才找到结算页"。

**调性关键词**（在第一版"手机优先/克制精确/单一强调色/工具感"基础上新增两条）：
- **余额优先**——欠了多少、该收多少，是打开行程第一眼看到的东西，不是要点导航才找得到的隐藏功能
- **谁欠谁清楚可见**——参与者不再只是一份"名单"，是一份"每个人跟我的账目关系"清单
- 其余延续第一版：手机优先单手操作、克制精确、单一强调色为主但允许分层、单列信息流、工具感而非消费品感

## 参照

分两层：**信息架构层参照**（这次新增，解决"首屏该长什么样"）和**视觉执行层参照**（第一版定的，继续有效，解决"颜色怎么用/数字怎么排"）。

### 信息架构层（新增）

**1. Splitwise——参照"余额优先"这个首屏结构，不是抄它的视觉风格。**
独立核实（Splitwise 2021 v5.0 官方博客 + IXD@Pratt 设计评论系列 + 两篇独立 UX 案例研究交叉验证）：改版后首屏给更大空间展示总余额，用户"总是知道自己的总余额"，原话是"You are owed $40 overall"配合"Ana and Bob each owe you $20"这种按人拆解的清单紧跟在后面。这次要照搬的具体结构是：**总额先出现，按人拆解的清单紧跟其后**，不是"列一堆数字自己算"。群组用圆角矩形、好友用圆形头像做区分；结算用"两个头像 + 中间箭头"表示钱从谁流向谁，比纯文字"A → B: ¥X"更直观——这条也要落地到本项目的转账清单。

**2. remy-thailand.pages.dev（Remy 自己认可"好用"的实现，390×844 实拍截图核实过）——参照三个具体手法，不是抄它的暖色配色。**
- 首屏顶部深色 hero 卡片：小标签在上、巨大数字居中、补充文字在下，就是"余额优先"落地到具体视觉形式的样子。
- 差异化功能（该项目的汇率比价）被当一等公民摆首屏，"最划算"选项用高对比底色 + 徽章高亮，不是塞进小字里——trip-expense-ledger 自己的比价功能也该这样处理。
- 空状态文案主动指向该去的地方（"点右下角 + 开始记账"），不是干巴巴一句"暂无数据"。
- **不照搬的部分**：暖色系配色（珊瑚红/藏青/金色/薄荷绿）、衬线斜体展示字体——那是 remy-thailand 作为旅行社交类 App 自己的品牌识别，trip-expense-ledger 是"把钱算准"的记账工具，照搬会显得在山寨而不是在做自己的产品。

### 视觉执行层（第一版定案，继续有效）

**3. Linear**——"整站一个强调色，其余全走灰阶，层级靠字重/靠明暗反转不靠加颜色"这条纪律继续用。这次 hero 卡片用深色反转（`slate-900` 底 + 白字）而不是新开一个第三色，就是直接套用这条纪律的产物。

**4. Stripe Dashboard**——金额数字 `tabular-nums` + 右对齐这套技法继续用，转账清单/参与者净额列表全部照这个来。

**5. Wise**——"钱的产品选强调色不必然是安全蓝"这条态度判断继续用，amber-700 这个强调色本身不用换。

## 绝对禁止

第一版列的 6 条执行纪律继续有效（精简措辞保留，不是重新论证一遍）：

1. **蓝紫渐变背景**——记账工具不是营销落地页，加渐变显得不严肃。
2. **emoji 当图标**——开源项目在不同系统渲染不一致，拉低"精确计算工具"的观感，要用就用 lucide-react 这类线条图标库（目前项目还没装，这次如果要做头像/图标需要新增依赖）。
3. **系统默认字体栈**——已经接了 `next/font/google` 的 Inter，不能退回浏览器兜底字体。
4. **Tailwind 默认蓝 `bg-blue-600`**——理财类产品扎堆"安全蓝"最没记忆点，继续用 amber-700。
5. **每个新区块都套同一个白盒子**（`rounded-md border border-slate-200 bg-white p-3`）——层级要靠字重/字号/明暗反转做出来，不是靠"再套一层卡片"。这条这一轮尤其关键：hero 卡片如果也套跟参与者列表一样的白盒子样式，等于白做了信息架构分层。
6. **给按钮加浮夸 hover 动效**——触屏本来就没有 hover，这条继续禁。

**这一轮新增第 7 条，是这次翻车的真正根因，权重最高**：

7. **只做视觉皮肤当成"设计已经做完"**——这正是第一版栽的坑：颜色对了、字体对了、间距对了、ui-auditor 也过了，但 Remy 说"完全没有从使用者角度出发"。因为视觉纪律再工整，如果首屏该放的信息压根没放（总余额、按人拆解的欠款），或者信息藏在要点两次导航才到的页面里，用起来还是不顺手。**审美关和信息架构关是两件不同的事，都要过，只过一个不算完成**——这是给以后任何"大改视觉"的任务提前打的预防针，不只是这次改完就完事。

## 信息架构与页面结构（本轮核心，逐页面/逐区块点名）

### 行程主页 `app/trips/[tripId]/page.tsx`——重排成 4 段，自上而下：

**① 余额 Hero 卡片（新增，第一个内容区块，紧跟标题下方）**
- 容器：`bg-slate-900 text-white rounded-lg`（不是继续用白底灰边框，用深色反转制造"这是本页最重要的信息"这个视觉信号，直接套用 Linear 那条"层级靠明暗不靠加颜色"的纪律，不新开第三个色系）
- 内容从上到下：小字标签（uppercase, `text-xs text-slate-400`，比如"我的净额"）→ 巨大数字（`text-5xl sm:text-6xl font-semibold tabular-nums tracking-tight`，正值该收用 `text-emerald-400`、负值该付用 `text-red-400`——注意这两个色号比列表里用的 `emerald-600`/`red-600` 浅一档，是因为深色底上需要更亮的色相才够对比，不是随手换的）→ 一行补充上下文小字（比如"3 笔待结算"）
- 卡片下方一行链接"查看结算明细 →"，指到现有 `/settlement` 页——hero 卡片是**入口预览**不是替代品，结算页那套完整的转账清单/标记已结算按钮继续留在那里
- 数据来源：需要把 `settlement-query.ts` 里的 `loadSettlementInput` + `computeNetBalances` 挪来在这个页面也调用一次（现在只有 settlement 页面在算），取当前 identity 对应的那一项净额

**② 参与者列表——从"认领状态"升级成"净欠款清单"**
- 每行结构改成：头像（圆形，`bg-slate-200 text-slate-700` 首字母，32px）+ 姓名（+ "创建者"小标签，如现在）+ **这个人相对当前用户的净额**（右对齐，`tabular-nums`，正值 `text-emerald-600` /负值 `text-red-600`，跟结算页现有配色逻辑保持一致）
- "已认领/未认领"状态不删，降级成姓名下方一行小字辅助信息（比如灰色小字"邀请待认领"），不再是这一行唯一显示的数据——现在这行唯一的信息是认领状态，改完后认领状态变成次要信息，净额变成主要信息
- 数据来源同 hero 卡片，一次查询两处复用

**③ 消费活动流（原"我的消费记录"改名 + 改数据范围）**
- 标题从"我的消费记录"改成"消费记录"或"活动流"
- **查询条件需要去掉 `enteredByParticipantId` 这个过滤**，改成显示整个行程的消费，不只是当前用户自己录入的——这是这次改动里唯一涉及后端 query 的一处，是让"同行人分摊"这件事在信息架构上成立的必要改动，不是可做可不做的视觉选项。改完之后每张卡片需要多显示一个"谁垫付的"字段（现有 `expenses` 表已有 `enteredByParticipantId`，只是取出来显示，不用改 schema）
- 卡片视觉：现有 `rounded-md border border-slate-200 bg-white px-3 py-2` 结构不用大改，加一行显示垫付人姓名（小字，配合头像更好但非必须，头像组件已经在②做了可以直接复用）

**④ FAB——保留现有隐藏逻辑和安全区处理，只改视觉呈现**
- 现有 `record-expense-fab.tsx` 的 `usePathname` 判断（在 `/expenses/new` 或 `/edit` 页隐藏）和 `env(safe-area-inset-bottom)` 处理**原样保留**，这两块工程实现是对的
- 视觉从"贴边整宽度按钮条"（`fixed inset-x-0 bottom-0` + `w-full`）改成**浮动定位的圆角胶囊按钮**：`fixed bottom-[calc(1rem+env(safe-area-inset-bottom))] right-4`，不再贴左右两边，宽度收缩成 `inline-flex` 自适应内容宽（图标 + "记一笔消费" 文字），`bg-accent-700 text-white rounded-full shadow-lg`，最小高度依然是 44pt（实际建议 52-56px，留一点余量让它看起来够"重"，配得上"全站最高频操作"这个身份）
- 这么改而不是照搬 remy-thailand 纯图标圆形按钮的原因：本项目只有一个高频操作（remy-thailand 有"记消费"和"换汇充值"两个，所以能各用一个纯图标区分），只有一个操作时保留文字标签能避免"这个圆点是干嘛的"的猜测成本，同时浮动定位（不贴边、有阴影）比现在的贴边通栏更贴近"这是一个悬浮的主操作"而不是"这是页面底部导航栏"的观感

### 结算页 `app/trips/[tripId]/settlement/page.tsx`——小改，复用①②做的组件

- "每人净值"列表：直接复用行程主页②新建的头像组件（同一批参与者数据），跟主页视觉语言保持一致，不用重新发明
- "转账清单"：从现在的纯文字"A → B：¥X"，改成 Splitwise 那种"头像 + 箭头 + 头像"的结构——`[fromAvatar] [fromName] → [toAvatar] [toName]`，金额右对齐另起一行或同行右侧，比纯文字直观。这是复用①②已经建好的头像组件做的小延伸，不是新开一套东西

### App 首页 `app/page.tsx`（已登录/多行程列表状态，`MyTrips` 组件）——次要建议，非本轮必做

- 每张行程卡片可以顺手加一个当前用户在该行程的净额小标签（复用同一个 `computeNetBalances` 逻辑，按行程各查一次），让"要不要点进去看"这个判断提前到列表页就能做——这条优先级低于上面①-④，是同一条"余额优先"原则的自然延伸，工作量允许的话顺手做，不做也不影响这轮改动的核心诉求

### 记一笔消费表单 `app/trips/[tripId]/expenses/expense-form.tsx`——比价推荐徽章升级

- 现有结构（比价区块本身是独立卡片、逻辑对）不用大改，只升级"最划算"那一行的视觉权重：从现在的 `bg-emerald-50` 底色 + `text-xs` 小字"最划算"，改成一个真正的徽章——`inline-flex items-center gap-1 rounded-full bg-emerald-600 px-2 py-0.5 text-xs font-medium text-white`，配一个小勾选图标（需要 lucide-react 的 `Check` 图标，见"绝对禁止"第 2 条提到的依赖新增）
- 这是直接落地调研发现里 remy-thailand"推荐徽章"手法的地方，颜色复用已有的 `emerald-600`（跟"该收"用的绿色同一个语义色，不新开色号）

## 视觉决定

- **色板**：延续第一版 `amber-700`(`#B45309`) 为唯一强调色/CTA 色，中性灰阶继续用 slate。**这一轮明确允许的分层，仅限这两处，不再扩大**：①hero 卡片用 `slate-900` 反转底（深色/浅色两态，不是新色相）②hero 卡片内的正负向数字用 `emerald-400`/`red-400`（比列表里用的 600 号浅一档，因为背景变深了，语义色本身没变）。财务语义色维持 `emerald-600`=正值该收/`red-600`=负值该付，推荐徽章复用同一个 `emerald-600`。**不新增任何跟 amber/emerald/red/slate 无关的第五种色相**——remy-thailand 的珊瑚红/藏青/金色/薄荷绿这套多色系不搬过来，前面"参照"一节已经说明原因。

- **字体**：继续只用 Inter 一种字体，**不加第二种展示字体**。这是判断题，我的判断是：remy-thailand 用衬线斜体是它作为"旅行/生活方式" App 的品牌识别，trip-expense-ledger 定位是"把钱算准的工具"，加一层装饰性字体风险大于收益，容易冲淡"精确/可信"这个第一版就定好的调性，也会被外部开发者读成"在模仿另一个产品"而不是"这个项目自己的态度"。**用字号和字重制造"个性"这一层，不用第二种字体**——具体做法是 hero 卡片的数字用 `text-5xl`/`text-6xl`，比全站现有任何文字都大得多，这个尺度反差本身就是"个性"，不需要额外换字体。

- **圆角**：维持第一版的小圆角 `rounded-md`（约 6px），hero 卡片可以用 `rounded-lg`（约 8px，比普通卡片略大一档但仍在"正式/精确"这个量级里，不跳到 `rounded-2xl` 那种气泡感）。**唯一的例外是 FAB**，浮动胶囊按钮用 `rounded-full`——这是因为浮动按钮（不贴边、有阴影、独立于页面网格）跟页面里排版的信息区块是两类不同的视觉物件，业界通行的浮动主操作按钮都是全圆角，这条不算破坏"小圆角=精确"的纪律，是给"这是一个悬浮的特殊操作"专门留的例外，且只有这一处。

- **间距密度**：维持第一版判断，单列信息流为主，不做多列表格。**这一轮新增的具体量级**：hero 卡片内边距给宽松一点（`p-6` 起步，比普通白盒子卡片的 `p-3` 明显宽敞），让它在视觉上"呼吸感"更足、更像这一屏最重要的东西；参与者列表和活动流卡片内部行间距不用因为多加了头像/金额就压缩，`gap-2`/`gap-1` 维持现有量级，真正要收紧的是头像跟文字之间的贴合度（`gap-2` 左右够用），不是整行的疏密。

## 跟系统里其它项目的关系

延续第一版判断不变：**刻意不跟 Remy 系统内部项目（SUI 珠宝系统/gem-deploy 那套暗色金色调）的 design language 走**，原因也不变——这是公开 GitHub 开源项目，受众是要自行判断要不要自托管的陌生开发者，不是 Remy 自己团队内部工具，调性判断继续全部来自外部真实产品（这轮新增 Splitwise 官方博客 + remy-thailand 实机截图）+ 官方移动端设计规范，不套用内部系统视觉语言。

这一轮唯一新增的"系统内参照"是 **remy-thailand.pages.dev 本身**——虽然它是 Remy 自己的另一个项目，但这里参照的不是"接近内部 design language"意义上的统一，而是把它当成一个**外部验证过好用的产品案例**来学具体手法（hero 卡/推荐徽章/浮动主操作），跟"跟内部系统保持一致"是两回事，前面"参照"一节已经说清楚哪些学、哪些不学（不学它的暖色系配色和展示字体）。
