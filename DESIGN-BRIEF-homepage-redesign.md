# trip-expense-ledger 首页（我的行程）设计简报 — 内容与排版专项

> **范围边界（先说清楚，别越界）**：这次只管首页（`app/page.tsx` 已登录分支 + `app/my-trips.tsx`）的内容该放什么、排版怎么组织、信息密度和层次感怎么设计。**不碰配色**——配色候选（`DESIGN-BRIEF-color-directions.md` 里的方向一/二/三/四/五提案）是另一条并行线，这份简报里凡是提到颜色的地方，都是"沿用现有语义色 token 不改值"或者"占位描述，等配色定案后再套用"，不指定新色号。也不碰真实数据接口/状态管理/性能，只出方向，不写代码，不改任何项目文件。
>
> **这是旧项目的局部改版，不是新项目**：`trip-expense-ledger` 已经有一套走了八版+若干补丁验证过的视觉方法论（`DESIGN-BRIEF.md`）和紧凑尺寸基准（`user_compact_ui_preference.md`）。这份简报的每一条判断都优先复用这两份文件里已经验证过的手法，不重新发明一套，也不推翻已经定案的东西。

## 先说一个跟任务描述对不上的地方（诚实标注，不隐瞒）

任务描述里说现有卡片包含"总消费汇总（如'总消费 ฿12,400 · RM1,020 · 6 笔'）"这一行，但我实读了当前仓库里真实的 `app/page.tsx` 和 `app/my-trips.tsx`（以及 `lib/db/user-trips-query.ts` 的查询逻辑），**这一行在已提交的代码里不存在**。当前 `MyTripCard` 只有三行内容：

1. 行程名称（`trip.name`，`font-medium`）
2. 副信息："本位币 {baseCurrency} · {创建者/同行人} · {记账中/已结算/已归档}"（`text-xs text-muted`，裸文字，状态是纯文字不是 pill）
3. 净额（`该收`/`该付` + 金额，`text-sm font-medium`，`emerald-600`/`red-600`）

卡片容器是 `rounded-xl border border-sand bg-paper p-4`，**没有任何阴影 token**（对比黄金路径其它清单——`tx-item`、`wallet-grid`、`trip-switcher` 弹层——都已经补上 `shadow-card`，见 `DESIGN-BRIEF.md` 第六/七/八版）。空状态目前只有一行纯文字："还没建过行程，点上面「创建新行程」开始。"

我判断"总消费汇总"这行大概率是 Remy 口头描述时结合了某个还没落地到代码里的设想，或者是记错了别处（比如行程详情页 Hero 卡本身有"总消费"信息）。**这份简报按任务描述给的完整版本（含总消费汇总）去设计，把它当成"这次要补齐的内容"而不是"已经有、只是排版问题"**——反正这条本来就是下面要建议新增的东西，不影响结论方向，只是澄清一下现状基线，免得 frontend-dev 照着这份简报去找一行本来就不存在的代码。

## 现状诊断：单调具体单调在哪

**1. 两张卡片之间零视觉差异化，信息呈现方式完全一样**
不管这个行程该收 RM50 还是该付 RM5000，不管是"记账中"还是"已结算"，卡片长得一模一样——大小相同、颜色权重相同、字号相同。净额数字虽然用了 `emerald-600`/`red-600` 区分该收该付，但这是唯一的差异化信号，且藏在卡片最后一行，眼睛扫过去第一眼落在的是最不重要的行程名称，不是"这个行程现在要不要我操心"这件事。

**2. 顶部大片信息真空，"我的行程"这四个字承担了本该属于"聚合信息"的视觉位置**
`h1` 只是一个功能性标签，不表达任何实际状态（有几个行程、总共该收该付多少、有没有需要处理的事）。对比行程详情页（黄金路径）—— 那里 Hero 卡片一进去就用大号净额数字告诉你"这个行程我现在该收该付多少"，首页作为"所有行程的入口"，反而没有一个对应的"整体状况一眼看清"的信息层，这是这次任务描述点出的"内容太少"最直接的体现。

**3. 卡片本身零光影质感，是纯色块堆砌，而且这个坑黄金路径已经在别处填过，唯独首页没接上**
`DESIGN-BRIEF.md` 第六版的核心论证是"哪怕是最克制的界面，'这是一个立起来的卡片'这件事也要靠阴影/描边这些光影线索去表达，不能只靠一块纯色"（原话，参照 suimuse 的 `--shadow-sm`/`--shadow-elevated`）；第八版又把这条手法补到了 `payment-methods`/`invites`/`settlement` 三个二级页面的清单行上（补 `shadow-card`）。**首页的行程卡片是目前唯一一个还没接上这条手法的清单类元素**——`rounded-xl border border-sand bg-paper p-4` 跟第六版之前的旧版清单行一模一样，是这条方法论在整个项目里最后一个没扫到的角落。这不是主观判断，是拿项目自己已经验证过的规则去对照代码，缺口是可验证的。

**4. 空状态完全没设计过，一行纯文字应付了事**
`trips.length === 0` 时只有一句"还没建过行程，点上面「创建新行程」开始。"——这是所有页面里唯一一个"什么都没发生"的界面状态，之前八轮改版全部在打磨"有内容时怎么呈现"，这一块从来没被纳入过讨论范围，任务描述专门点出这一点是对的。

**5. 多行程堆叠没有分组/排序逻辑，`grid grid-cols-1 sm:grid-cols-2` 是纯瀑布流**
`trip.status` 这个字段本来就有 `active`/`settled`/`archived` 三态（`DESIGN-BRIEF.md` 第五版还专门给这三态设计过 `ok`/`live`/`wait` 状态色 pill，但目前首页卡片渲染的是裸文字状态，没接这套 pill——这也是一个具体的、可验证的缺口），但查询和渲染完全没利用这个字段做分组，两个"记账中"的行程和一个"已结算"的行程会被同等对待混在一起。

## 定位

**一句话**：打开首页应该像翻开一本随身带的记账本封面——先知道"这几趟出差我现在欠谁、谁欠我"，再决定要不要点进哪一本细看，不是一张平铺的功能菜单。

**调性关键词**（延续 `DESIGN-BRIEF.md` 第三版定的五条整体调性，这次针对首页场景收窄成跟内容/排版直接相关的几条）：
1. **余额优先延伸到列表层**——这条第三版就定了（"每张行程卡片顺手标一下当前用户在这个行程里的净额"），这次要做的是把它做实：不只是"卡片上有一行净额文字"，而是"扫一眼整页就知道自己现在整体是该收还是该付"。
2. **紧凑但有辨识度**——每张卡片信息密度可以比现在略高（加状态 pill、加最近消费预览这类信息增量），但字号/padding 必须落在 `user_compact_ui_preference.md` 的紧凑基准内，不能靠"加大留白"制造层次感。
3. **有温度但不是营销首页**——欢迎语/统计摘要要像"记账本翻开第一页的小结"，不是电商 App 首页的推荐 Banner。

## 参照

**首选参照：项目自己的行程详情页 Hero 卡片（`app/trips/[tripId]/page.tsx`）**——这是这次最合适、也最省论证成本的参照对象，因为已经跟 Remy 来回磨了八轮，方法论已经验证过，不需要再引进新参照。具体参照它的三件事：
- **"余额优先"的信息层级**：净额数字永远是页面里字号最大、位置最靠前的元素，其它信息（币种、状态、笔数）都是围绕它的辅助说明，不是并列关系。首页目前三行信息是并列堆叠（名称、副信息、净额同等视觉权重），应该改成跟 Hero 卡一样的主次分明。
- **"卡片要有光影质感"这条已验证原则**（第六版原文引用 suimuse 的判断依据："即使用浅色克制配色，卡片依然靠 shadow-sm/shadow-elevated/border 建立立体感，不会让任何卡片变成纯色块"）——首页卡片补 `shadow-card`（跟 `.tx-item`/`wallet-grid`/`trip-switcher` 弹层用的同一个 token，不新造）。
- **状态用 pill 不用裸文字**：第五版已经定案 `ok`(完成)/`live`(进行中)/`wait`(待定) 三态色 pill，且明确写过"呈现形式必须刻意区分：财务金额固定裸文字色不加底色块，状态固定带底色 pill"——首页卡片副信息里的"记账中/已结算/已归档"目前是裸文字，应该换成这套已经设计好、只是没被首页接上的 pill 系统（`active`→`live`，`settled`→`ok`，`archived`→复用 `text-muted` 灰阶降级，不强套 pill，跟 `DESIGN-BRIEF.md` 第五版"已过期/已撤销不强套三态色"是同一个判断）。

**次要参照：`wallet-grid.tsx` 钱包卡片的"多实例同类卡片"排布逻辑**——这个组件解决的是跟首页高度相似的问题："同一屏内并列展示多个同类实体，且数量因人而异"。参照它两件事：①用 emoji/图标 + 名称同一行做身份锚点，不是纯文字堆砌；②末尾追加一张视觉上明显不同（`border-dashed`）的"新建"卡片，跟内容卡片区分开——首页"创建新行程"目前是顶部一个孤立按钮，如果未来想让它更融入列表本身（比如列表末尾也放一个新建入口），可以参照这个手法，但**这条不是必须**，顶部按钮已经够用，只是提供一个备选思路。

**没有采用外部产品做参照**：任务范围明确排除配色，而首页这次要解决的问题（信息层级、分组、空状态）本质是"怎么把项目自己已经验证过的手法用到一个还没用上的角落"，不是"要不要引进一种全新的排版语言"，所以这次不需要 remy-thailand 或其它外部案例，参照对象就是项目自己。

## 绝对禁止

**1. 别把统计摘要区块做成第二个 Hero 大色块，跟下面的卡片列表抢焦点**
反例：在标题下面加一整块跟 Hero 净额卡同等视觉重量的深色实底摘要卡（大字号 + 大 padding）。为什么不行：首页的核心内容是"行程列表"，摘要只是辅助信息，如果摘要本身做得比任何一张行程卡都显眼，用户会先看摘要再看列表，主次颠倒；摘要应该是一行紧凑的文字/小型信息条，视觉权重明显低于任何一张具体的行程卡。

**2. 别默认放一个没有具体意义的装饰性 icon**
反例：在统计摘要区块或空状态旁边放一个通用的钱包/图表/趋势线之类的 stock decorative icon。为什么不行：`user_compact_ui_preference.md` 2026-09-10 明确记过这个具体教训——Hero 卡右上角一个硬币图标被 Remy 直接说"不喜欢换掉"，往后设计草稿默认不放这类元素，除非有明确意义。**这条不等于禁用 emoji**——项目自己的图标语言是"emoji 锚定具体内容类型"（第三版定案，参照 remy-thailand），比如钱包卡片用 emoji 标钱包类型，这是有信息功能的用法；如果要用 emoji 也必须绑定具体信息（比如用行程本位币对应的国旗/货币符号做身份锚点），不能为了"看起来不单调"随手塞一个泛泛的旅行/记账主题装饰图。

**3. 别为了"不单调"塞跟记账无关的营销/推荐内容**
反例："猜你可能想建的行程模板""邀请好友得奖励""本周消费小贴士"这类内容。为什么不行：`CLAUDE.md` 项目定位写得很清楚，这是"私有部署，Remy 自己每天用的记账工具"，不是要拉新留存的 SaaS 产品；塞这类内容会让首页从"我的账本"变成"一个想让我多用几次的 App"，跟第三版定的"有温度的私人记账本，不是企业仪表盘"这条调性直接冲突。

**4. 别用大字号/大留白做欢迎区，把首页做成移动端营销落地页**
反例：给统计摘要用 `text-2xl`/`text-3xl` 起步的大字号，或者给欢迎语单独留一大块 `py-8` 级别的呼吸空间。为什么不行：`user_compact_ui_preference.md` 记录的 2026-09-08/09-10 两次复发都是同一个坑——"字号大/留白多/触控热区宽"这套是移动端营销页审美，Remy 明确反馈过"全部东西都很大很占位子"。统计摘要这类新增内容，字号应该落在跟"分节标题"同一档（`text-[12.5px]`）或更小，不能因为是"新加的欢迎区"就单独破例放大。

**5. 空状态不要做成儿童向插画+大 emoji 的"温馨提示卡"**
反例：一个居中的大号 🧳 或插画配"还没有行程哦，点击下方开始你的第一次记账之旅吧！"这种偏可爱、偏营销的文案和排版。为什么不行：`DESIGN-BRIEF.md` 从第三版起就确立了"有温度但克制"这条基调，不是"卖萌"；空状态是首页的一个正常状态，不该单独放宽审美标准去讨好用户，文案可以比现在更有一句人话的引导感，但排版和字号要跟其它内容保持同一套克制纪律。

**6. 别给卡片间的差异编造没有数据支撑的装饰性颜色**
反例：给每张行程卡片随手挑一个不同的强调色/边框色做"视觉区分"，纯粹为了"看起来不单调"。为什么不行：`DESIGN-BRIEF.md` 第三版"绝对禁止第 8 条"早就定过"颜色变多之后每种颜色必须绑定固定语义，乱用会让'多色'从'信息更清楚'变成'花但看不懂'"——卡片间的视觉差异必须由真实数据驱动（净额正负、状态 active/settled/archived），不能靠主观分配颜色制造差异感。

**7. 别把消费记录明细带上首页卡片**
反例：卡片上直接列出"最近 3 笔消费"的具体金额和分类。为什么不行：`CLAUDE.md` 权限边界写得很明确，"逐笔明细默认私密"，首页这一层是跨行程的列表视图，只适合展示已经确认可以公开给自己看的聚合信息（净额、笔数、状态），"最近一笔消费预览"如果要做，也只能是极简的一行摘要（比如"最近：8 月 20 日"这种时间戳级别的信息），不能带出具体消费内容明细，避免信息密度和隐私边界一起失控。

## 视觉决定

> 这部分只给内容/排版/信息架构的具体决定，不含新色号（配色沿用现有语义色 token，见下方说明）。

**内容结构（这是这次最核心的交付）**：

1. **顶部欢迎+统计摘要行**（新增，取代现在"我的行程"这个纯标签）：
   - 保留 `h1`"我的行程"（继续 `text-base font-semibold text-ink`，不改字号，它现在的字号已经符合紧凑基准，不需要放大）
   - 紧跟其下加一行聚合摘要，格式类似"3 个行程记账中 · 净该收 RM1,284.50"，字号 `text-[10px] text-muted`（跟现有"我的账号"链接同一档），**不单独起一个大字号新层级**
   - 摘要的净额计算逻辑：把当前用户在所有 `active` 状态行程里的净额加总（用绝对金额做符号运算，正负分别对应该收/该付，展示时套用现有 `emerald-600`/`red-600` 语义色，不新增颜色），`settled`/`archived` 的行程不计入这条汇总，因为已结算的钱不该再占用"现在该操心多少钱"这条信息
   - 如果只有 0-1 个行程，这行摘要可以省略或改成更简短的一句话（比如只有 1 个行程时摘要跟单张卡片信息重复，没必要重复展示），具体阈值判断交给 frontend-dev

2. **卡片分组**（新增，解决"堆叠列表"问题）：
   - 按 `trip.status` 分两个视觉层级，不是三个并列分组：
     - **"进行中"组**（`status = 'active'`）：正常权重展示，排在最上面，组内默认按净额绝对值从大到小排序（欠得越多/该收越多的越靠前，延续"余额优先"原则到排序逻辑本身，不是只延续到单卡片展示）
     - **"已结算/已归档"组**（`status = 'settled' | 'archived'`）：降级展示，用一个不显眼的分节小标题（`text-[10px] text-muted`，比如"已结束"）隔开，卡片本身可以整体降低视觉权重（比如用 `opacity-70` 或去掉阴影只保留细边框），不需要额外的展开/收起交互（这不是团队看板那种需要"归档隐藏"的场景，数量通常不多，直接平铺展示但视觉上降级即可，不用做成可折叠区块增加交互复杂度）
   - 如果只有一组有内容（比如全部都在记账中），不显示分节标题，避免"标题下面空空如也"的空组

3. **单张卡片内容升级**（在现有三行基础上增补，不是推翻重来）：
   - 第一行：行程名称，不变
   - 第二行：把"本位币 XX · 创建者 · 记账中"这行拆开——币种代码可以做成一个小 chip（比如 `text-[9.5px]` 级别的小标签，参照汇率比价卡已有的 chip 用法，不新造样式），角色（创建者/同行人）保留裸文字，**状态从裸文字换成 `live`/`ok` pill**（对齐 `DESIGN-BRIEF.md` 第五版已经设计好、其它地方已经落地的状态色系统，首页只是接上这套现成的东西，不是发明新的）
   - 第三行：净额，保持现有的裸文字+语义色处理方式不变（这条本身没有问题，第五版明确说过"财务金额固定裸文字色不加底色块"，不能因为想让卡片更丰富就把净额也做成 pill，那会跟状态 pill 的视觉语言混淆）
   - **新增第四行（可选，视觉权重最低）**：总消费汇总，格式参照任务描述给的"总消费 ฿12,400 · RM1,020 · 6 笔"，字号 `text-[10px] text-muted`，跟第二行副信息同一档甚至更弱，**这行的作用是信息增量，不是视觉焦点**，摆在卡片最下面
   - 卡片容器补 `shadow-card`（对齐 `.tx-item`/`wallet-grid` 已有的处理），padding 维持现有 `p-4` 或按紧凑基准评估是否需要收到 `px-[9px] py-2` 级别——**这条留给 frontend-dev 参照 `user_compact_ui_preference.md` 卡片 padding 5/9px 这条基准去核实**，这份简报只钉"要有阴影质感"这个结论，不重新核算具体 padding 数值（那是尺寸密度维度，不是这次内容/排版专项的核心）

4. **"最近一笔消费预览"——加分项，非必做**：
   如果要加，只能是极简的一行时间戳级别信息（比如"最近更新：8 月 20 日"），**不展示具体消费内容/金额明细**（见绝对禁止第 7 条的隐私边界）；且这行信息要跟第四行"总消费汇总"合并考虑，不要让卡片同时新增两行低权重信息导致卡片整体变高、违反紧凑基准——如果两者都要加，优先级是"总消费汇总"高于"最近一笔预览"，后者可以先不做。

5. **空状态**（新增设计，目前完全没有）：
   - 文案从"还没建过行程，点上面「创建新行程」开始。"改成更完整的一段引导（可以补一句说明这个 App 解决什么问题，呼应 CLAUDE.md 里"出差记账+同行人代垫结清+汇率比对"这几个卖点中的一两个，让第一次打开的人知道这东西是干嘛的），但**排版规格跟正常内容页一致**，不单独放大字号或加插画
   - 不需要新增图标/插画（见绝对禁止第 5 条），如果 Remy 后续觉得太素想加一点视觉元素，建议先看实际效果再决定，不在这份简报里预先钉死一个图标方案
   - "创建新行程"CTA 按钮保留现状（`.btn-primary`），不需要因为空状态而改变按钮样式

**色板**：不在这次讨论范围内。沿用现有财务语义色（`emerald-600` 该收 / `red-600` 该付）和状态色 pill（`ok`/`live`/`wait`，第五版已定案），这几个 token 的具体 hex 值等 `DESIGN-BRIEF-color-directions.md` 那条线定案后统一替换，这份简报只管"用不用 pill、用不用语义色区分"这个结构性决定，不管具体颜色数值。

**字体**：不新增决定。净额数字继续用 `font-serif`(Fraunces) + `tabular-nums`，遵守既定纪律（只用于阿拉伯数字，不碰中文内容），跟卡片其余中文内容一致用 Inter，这条不因为这次改版而变化。

**圆角**：沿用现有 `rounded-xl`(12px) 标准卡片圆角，不新增新的圆角档位。

**间距密度**：新增的摘要行/分节标题/pill/第四行汇总信息，字号一律落在 `user_compact_ui_preference.md` 紧凑基准里已有的档位（`text-[12.5px]` 卡片标题级 / `text-[10px]` 副信息级 / `text-[9.5px]` chip 级），**不引入任何比现有卡片名称字号更大的新字号层级**——这条是这次最容易翻车的地方，务必守住，往下页写这条到"绝对禁止"里已经强调过，这里再钉一次具体数值边界。

## 跟系统里其它项目/黄金路径的关系

这次全部内容都是"把项目自己黄金路径已经验证过的手法，接到首页这个还没接上的角落"，不引进任何新的视觉语言，也不新造任何新组件规格：
- **状态 pill**（`ok`/`live`/`wait`）——已经落地在 `settlement/page.tsx`/`invites-manager.tsx`，首页这次是补齐第三个还没接的地方
- **`shadow-card` 阴影 token**——已经落地在 `.tx-item`/`wallet-grid`/`trip-switcher`，首页行程卡片是目前唯一一个还没补上的清单类元素
- **"余额优先"排序逻辑**——目前只体现在"每张卡片显示净额"这个展示层面，这次延伸到"列表本身按净额排序、按状态分组"这个排列层面，是同一条原则往深一层的自然延伸，不是新原则
- 跟"配色候选"那条并行线完全解耦：不管最后选哪个配色方向，这份简报的内容结构、分组逻辑、pill/阴影的使用决定都不需要跟着改，只有具体 hex 值需要替换，这是这份简报刻意保持的一个边界。

这次是延续第三版以来"照搬 X 风格＝读 X 真实代码逐值对齐，不是凭印象照猜"这条纪律，这次的"X"是本项目自己的黄金路径页面和 `DESIGN-BRIEF.md` 已经定案的方法论，不是外部参照——跟第八版处理三个二级页面技术债时是同一个性质的任务。
