# PARITY-CHECKLIST — Artifact V10 逐屏逐子区块对齐清单

这份文件是活文档，不是这轮用完就丢的一次性产物。以后每一轮改动/审计都要回来更新这份清单，不能只改代码不更新这里。

## 为什么要有这份文件（2026-09-17 第二十一轮起）

Remy 在第二十一轮问了一句关键的话："你为何没有一一的跟着方案次序 01-09 这样审核？"——过去几轮的审计习惯是"截图 + 肉眼扫一遍 + 写文字结论"，没有一份持久化、按方案顺序、拆到子区块粒度的检查清单，导致每一轮容易：
1. 只核对 Remy 这次点名的几个差异，其它区块凭"上一轮查过"的印象跳过，但方案本身也在演进（比如 HKD 候选这次的回归就是"之前对的东西后来又跑偏了"）。
2. 截图只截首屏可见范围，一屏往下滚动才看到的区块（比如屏 02 的"记录·HISTORY"）从来没被真的截全、真的拿去跟方案逐项比对过。

这份清单的用法：
- 按方案 01-09 的屏幕顺序，每屏下面列出所有独立子区块/子组件——不能写"屏02 ✅"就算审完，要拆到"Hero卡"/"我的钱包卡"/"汇率比价"/"记录·HISTORY"这个粒度分别打勾。
- 每一项标记：✅完全对齐 / ⚠️部分对齐（附差异说明）/ ❌不对齐（附差异说明）/ 🔲本轮未检查。
- 截图方式：每一屏都要截"完整长图"（full-page screenshot 或滚动拼接），不能只截首屏可见区域。
- 方案来源：Artifact V10（`https://claude.ai/code/artifact/86772aaa-6ddc-4fd6-bff1-798788f15d1b`），本地曾保存过一份完整源码快照供比对用（1432 行 HTML+CSS，含全部 9 屏 + notes 变更记录）。
- 真实数据来源：Remy 真实在用的行程"🇭🇰2026香港"（tripId `f78a6b5e-8612-4097-8bfd-88a5db664045`）——这是本项目专属铁律，任何一轮验收都不能只测 demo 行程。

---

## 第二十二轮验证结论摘要（2026-09-17）

Remy 对第二十一轮遗留的3个悬案都表态了（不再是"没决定"）+ 报了一个新的真实体验问题："手机上点击输入框会自动放大"（经典 iOS Safari font-size<16px 强制缩放行为）+ 要求安排一次全新独立的 parity 复核（不能再套用"round21已核对"这个信任）。

**4项代码改动**：
1. 结算屏转账清单头像——**去掉**（Remy 表态照方案字面来，round21的"保留更好用"判断作废）。
2. 汇率比价"渠道比价"——**接了真实中间汇率**，新增 `GET /api/trips/{tripId}/fx-mid-rates` + 共享模块 `lib/fx/rate-cache.ts`（复用"我的支付方式"那组本来就在用的 `exchange_rate_cache`/open.er-api.com，不是另起一套），各渠道 = 实时中间价 × 固定点差/手续费%。
3. 换汇（取款/换汇）**完整流程真的测到底了**——新建2个测试钱包、提交1笔真实换汇、D1直接查证双边余额+换汇记录数字全部正确、测完 `wrangler d1 execute` 清理干净（`SELECT count(*)` 验证0残留）。详见下方"round22 换汇完整流程实测"。
4. iOS输入框缩放——全站 `<input>`/`<textarea>` 补了 `font-size:16px` 的全局覆盖规则；**第一版有真bug**（`textarea` 裸标签选择器 specificity 不够，备注框实测还是10px），用真实 Playwright 脚本测出来后修对，重新实测全部28个字段=16px。详见下方"round22 iOS 输入框缩放实测"。

**独立全9屏复核**：这次是一个完全独立、跟做修复的不是同一个agent的 ui-auditor，从头重新核对了全部9屏（不因为清单写"round21已核对"就跳过），全部截图桌面+手机完整长图。结论：4项改动全部确认生效，另外两个之前标⚠️的条目（"我承担"分类明细表、换汇入口）这轮也补齐了实测证据升级成✅，发现一个跟round21记录不一致的地方（"复制链接"按钮反馈，见下方悬案1）。截图 `/Users/linotan/Desktop/Claude/.playwright-mcp/tel-independent-verify/`（21张）。

**验证方式说明**：这轮除了独立 ui-auditor 的截图走查，iOS缩放字号和换汇流程这两项改用了更精确的验证手段（真实 Playwright 脚本直接测 `getComputedStyle`、直接查 D1 数据库核对余额数字），比单纯截图肉眼比对更硬，也正因为这样才抓到了 textarea 那个真 bug——如果只信"代码看起来应该对"就不会发现。

---

## 第二十一轮验证结论摘要（2026-09-17）

**"记录·HISTORY 区块从没被真的检查过"这个猜测——验证结果：部分成立。**
查了 round18 的全页长截图 `audit-diffs/round18-full-parity/live/02-triphome-desktop-full.png`（`full` 后缀说明这是完整长图，不是只截首屏），这张图里确实拍到了"活动流"区块，能看到每行是"emoji+分类·商家 / 姓名+日期 / 金额"这个结构，编辑删除图标是常驻显示的。**所以截图本身是拍到了**，round18 的验收工作并不是完全没看这个区块。但从 PENDING-DECISIONS 第十七到二十轮的文字记录看，没有任何一轮的文字结论提到过"记录·HISTORY"标题下方的提示文案、每行的"≈约算金额"、操作按钮"点击/滑动才出现"这几个具体子项——**截图存在，但从没有人把这张截图里的这个区块，逐项拿去跟方案的 `.hist-static-note`/`.hist-approx`/`.hist-actions{transform}` 这些具体规则核对过**。也就是说盲区不在"没截图"，而在"截了图但没有逐子区块核对的清单，导致图里已经暴露的差异没人真的去比对方案"——这正是这份清单要根治的问题。

**本轮修复的问题（详见下方对应行）**：
1. 汇率比价目标币种候选缺 HKD、多 MYR——根因是候选逻辑从"写死清单"变成"从 FX_RATES 表 key 里推导"，而 HKD 那一行数据表本身没有自己对自己的 key，副作用漏出了 MYR。改回字面写死的 THB/USD/SGD/CNY/HKD 五项清单。
2. 汇率比价"我持有"从固定 tab 改成跟"目标币种"一样的下拉——这是 Remy 这轮在方案基础上追加的新要求（方案demo本身"我持有"也是固定tab，这点如实记录是新判断不是方案原文）。
3. "记录·HISTORY"（活动流）区块整体重做：补充说明提示文案、每行结构改成"图标+名称/meta行(垫付人·日期·分类·支付方式)/约算金额"三层、操作按钮从常驻改成点击展开。
4. 结算屏 + 记一笔消费屏逐 CSS token 核对：`.list`圆角14px/gap 2px、`.p-row`gap 5px、`.avatar`字号公式、`.settle-detail`盒子样式、`.field`label-input间距2px、`.field-input`圆角10px、`.dd-fake`圆角10px、`.split-panel .btns`gap 5px/padding 5px 7px 等多处数值级偏差。
5. `CategoryCombobox`真实功能性 bug：选中一个分类后再次点开下拉，候选列表被过滤成只剩当前选中值——根因是过滤用的 query 直接读表单当前值（选中后 value 等于该选项，自我匹配把其它选项全过滤掉了），改成独立的 `filterQuery` 状态，只有打字时才收窄候选，点开/聚焦时清空显示完整列表。
6. 支付方式页两个列表（"已配置的支付方式"/"本行程启用的支付方式"）——独立 ui-auditor 走查发现用的是老 `.tx-item`/`rounded-xl` 个体描边胶囊，跟结算屏这轮统一的"共用 `.list` 容器 + 内部 `.p-row`/`.check-row`"不是同一套规格（圆角12px vs 14px、行间gap 8px vs 2px、内部字号也有差异）。查证 Artifact `#scr-payment` 源码确认这里也是共用 `.list` 容器，本轮已改成同一套写法。

**独立 ui-auditor 复核（跟做修复的不是同一个 agent）**：全 9 屏逐条走查，重点验证了本轮 5 项代码改动（分类下拉bug/HKD候选bug/我持有下拉/活动流重做/支付方式列表token），全部确认通过。详细逐屏结论见下方各表格，走查报告原文摘要：分类下拉在快速记账卡+记一笔消费页两处都验证候选数量完整、能重新选择；HKD候选在"我持有HKD/MYR"两种状态下都验证候选清单跟排除逻辑吻合；活动流点击展开/收起两个方向都验证了；截图目录 `/Users/linotan/Desktop/Claude/.playwright-mcp/tel-round21-verify/`（14张，含桌面/手机全页长图）。全程只读，未提交任何真实表单，走查中途行程被 Remy 本人实时新增了一笔"酒店tax"消费（非本轮测试产生，已确认自己没有点击过任何提交按钮）。Console 0 error（13个无害的字体preload警告）。

---

## 屏 01 · 首页 · 我的行程

| 子区块 | 状态 | 说明 |
|---|---|---|
| 顶部 topbar：标题"我的行程"+ 副标题（N个行程记账中·净该收/该付） | ✅ | ui-auditor 核实：只有1个进行中行程时故意不显示这行（避免跟唯一卡片信息重复），是有意的条件渲染，不是漏做 |
| "＋创建新行程"按钮 | ✅ | round19 已补"＋"前缀，本轮未改动 |
| 行程卡片：标题 | ✅ | ui-auditor 核实：`text-[12.5px]`，且 `DESIGN-BRIEF-homepage-redesign.md` 明确写"卡片标题级 12.5px"——之前清单猜测"应该15px"是猜错的，12.5px才是设计稿要的值 |
| 行程卡片：标题旁 ✎ 改名图标 + 弹出输入框 | ✅ | 已实现且是真功能（`app/my-trips.tsx`，点击调 PATCH 接口真的存档，不是demo假保存），本轮读代码确认+ui-auditor实测点开验证 |
| 三个标签 chip（币种/身份/状态） | ✅ | round19 统一成白底细边框灰字圆角样式 |
| 日期范围行（📅 09/11-09/15） | ✅ | ui-auditor 核实真实数据下显示"📅 09/14 - 09/19"，格式合理 |
| 金额行（该收/该付） | ✅ | 沿用既有实现 |
| meta 行（总消费·笔数） | ✅ | 沿用既有实现 |
| 字段顺序（标题→标签→日期→金额） | ✅ | round19 已改 |

## 屏 02 · 行程主页

| 子区块 | 状态 | 说明 |
|---|---|---|
| header（标题+副标题"本位币 X·记账中"） | ✅ | round17/18 已对齐 |
| "我的账号"/"退出登录"链接样式 | ✅ | round17 已对齐（10.5px+常驻下划线） |
| 切换行程下拉（trip-header-nav） | ✅ | round17 重写为文档流内嵌面板，非悬浮裁切 |
| navtabs（行程主页/结算/支付方式/邀请管理） | ✅ | round17 已修字号/去下划线 |
| Hero 卡：标签文案"消费总金额" | ✅ | round21 本轮改回方案原文（详见 PENDING 第二十轮记录） |
| Hero 卡：净额数字+正负色 | ✅ | 未改动 |
| Hero 卡："该收回/该付出·N笔消费" | ✅ | 未改动 |
| Hero 卡："我承担"分类明细表（不含机票/宝石/机票N/宝石N） | ✅ | **round22 升级为✅**——独立 ui-auditor 用真实数据核实：这条真实行程当前有餐饮/交通/住宿三类消费，明细表精确显示这三行（净HK$79/HK$50/HK$42）+汇总行，行数正确反映真实数据分布，不多不少 |
| 我的钱包卡（钱包列表+新增按钮+仅自己可见标记） | ✅ | ui-auditor 走查确认三色阶开关都在，无明显问题 |
| 我的钱包卡：**删除钱包** | ✅ | **round24 新增**——胶囊右上角小垃圾桶图标，接上既有DELETE端点，二次确认（有余额/有关联换汇记录时文案会明确警告后果），409（有关联换汇记录）时前端显示清楚的引导文字。独立 ui-auditor 走查坐实第一版删除图标热区仅13×13px（低于`DESIGN-BRIEF.md`第204行"toolbar icon 28×28px"基准），已修复到28×28px并用`boundingBox()`精确测量验证 |
| 换汇入口"💱取款/换汇" | ✅ | **round22 升级为✅**——这轮真的走完整流程测到底了，见下方"round22 换汇完整流程实测"一节，不再是只测外观 |
| 快速记账卡：字段顺序（分类→币种→金额） | ✅ | round14 已改 |
| 快速记账卡：分类下拉箭头/宽度 | ✅ | round19 已修脱节+超宽两个 bug |
| 快速记账卡：**分类下拉选中后重开只剩自己一项** | ✅ | **本轮修复**——`CategoryCombobox` 过滤逻辑 bug，见上方摘要第5点 |
| 快速记账卡：商家名称字段 | ✅ | round19 补上 |
| 快速记账卡：分摊三段式按钮（.seg3 米黄配色） | ✅ | round17 已对齐配色 |
| 快速记账卡：自定义分摊展开面板 | ✅ | round17 对齐米黄配色 |
| 汇率比价：目标币种下拉候选（THB/USD/SGD/CNY/HKD固定5项） | ✅ | **本轮修复**——改回字面写死清单，不再受"我持有"选择影响副作用漏出 MYR |
| 汇率比价："我持有"选择方式（下拉 vs 固定tab） | ✅ | **本轮改动**——应 Remy 要求从固定tab改成下拉，跟目标币种同一套交互；这是方案基础上的新增需求，不是方案原文本身要求（方案demo里"我持有"本来就是固定tab），如实记录 |
| 汇率比价：⚙自选渠道下拉 | ✅ | round18 已实现 |
| 汇率比价：基准换算卡片（1 X = Y） | ✅ | 未改动，逻辑对 |
| 汇率比价：渠道比价列表+最划算徽章 | ✅ | round18 已验证排序正确 |
| 汇率比价：说明脚注（渠道固定表vs我的支付方式实时） | ✅ | 未改动 |
| 汇率比价：**实时汇率更新** | ✅ | **round22 修复**——Remy 明确要求"要"，渠道比价也改接真实中间汇率了。新增 `GET /api/trips/{tripId}/fx-mid-rates`+共享模块 `lib/fx/rate-cache.ts`（跟"我的支付方式"那组同源，24小时刷新，来源 open.er-api.com），各渠道 = 实时中间价×固定点差/手续费%。独立 ui-auditor 实测：脚注文案已变成"中间汇率来自实时数据，更新于 HH:MM"，点"↻刷新"后时间戳从09:30变成实测当下的22:13，行为证据支持是真实请求（没能力直接看Network面板确认200，间接证据充分） |
| 记录·HISTORY（活动流）：说明提示文案 | ✅ | **本轮新增**——如实描述这个真实 app 自己的排序/筛选行为（不是照抄方案demo自身的免责声明） |
| 记录·HISTORY：排序下拉（手动/日期/金额） | ✅ | round20 已实现，全部真实生效 |
| 记录·HISTORY：4个筛选chip（分类/垫付人/日期/支付方式） | ✅ | round20 已实现 |
| 记录·HISTORY：每行结构（图标+名称/meta行/约算金额） | ✅ | **本轮重做**——名称优先显示商家名、meta行含垫付人·日期·分类·支付方式，约算金额挪到左栏 |
| 记录·HISTORY：操作按钮非常驻（点击/滑动触发） | ✅ | **本轮实现**——改成点击整行展开，金额让位、编辑删除滑入，对齐方案"点击或滑动二选一"的要求 |
| 换汇记录列表（仅自己可见） | ✅ | ui-auditor 核实空状态文案清楚，符合规范 |
| 换汇记录列表：**删除换汇记录（全新功能）** | ✅ | **round24 新增**——之前完全没有删除能力，这轮新增后端`DELETE /api/trips/{tripId}/exchange-records/{id}`+前端删除图标，删除时把这笔记录对双边钱包的净影响自动回滚（真实D1数据核实精确回滚，单测覆盖"删中间一笔不影响其它笔"）。删除图标热区同上一并修到28×28px |

## 屏 03 · 记一笔消费

| 子区块 | 状态 | 说明 |
|---|---|---|
| 标题 | ✅ | 15px，已对齐 |
| 金额+币种（row2） | ✅ | 圆角/间距本轮修正为 10px/2px |
| 商家名称字段 | ✅ | 已存在，本轮修 gap |
| 日期字段 | ✅ | 已存在，本轮修 gap |
| 分类字段（CategoryCombobox+chevron） | ✅ | round19 修过脱节，本轮修 gap；底层 bug 同上已修 |
| "这笔不计入分摊"勾选框 | ✅ | round14 刻意加的真功能，保留 |
| 支付方式下拉 | ✅ | 本轮修圆角为10px（继承 field-input chokepoint） |
| 备注字段（textarea） | ✅ | **round22 修复过一个真 bug**——iOS 缩放字号修复第一版漏了这个字段，`textarea` 裸标签选择器 specificity 不够高，实测聚焦后字号还是10px，没生效；补 `textarea.field-input` 选择器后重新实测=16px，见下方"round22 iOS 输入框缩放实测"一节 |
| 收据上传字段 | ✅ | round14 刻意加的真功能，保留 |
| "跟其他人split"开关 | ✅ | round17 已改成开关（非三段式） |
| split-panel 外壳（米黄背景/圆角12/padding8/gap7） | ✅ | 已对齐，本轮核实数值全部匹配 |
| "跟谁分？"参与者chip | ✅ | 本轮修 gap（6→5px）+ padding（改用 ctrl-pad 5px 7px） |
| "谁垫的钱？"下拉 | ✅ | 本轮修圆角（8→10px） |
| "怎么分？"平分/自定义chip | ✅ | 本轮修 gap+padding，同"跟谁分？" |
| 自定义分摊逐人金额输入 | ✅ | 未改动，2人联动逻辑保留 |
| 提交按钮（.big-cta 全宽） | ✅ | round19 已改全宽 |
| 取消链接（次要位置） | ✅ | round19 已降级 |
| field 组件整体（label-input间距2px） | ✅ | **本轮修复**——全部 `.field` 级 wrapper 从 gap-1(4px) 改 gap-[2px] |

## 屏 04 · 结算

| 子区块 | 状态 | 说明 |
|---|---|---|
| 标题 | ✅ | 15px |
| "每人净值"小标题（灰色大写风格） | ✅ | round18 已统一 |
| 净值列表容器（.list：圆角14/gap2/padding 3px 5px） | ✅ | **本轮修复**——圆角 12→14px、gap 4→2px |
| 净值行（.p-row：gap 5px） | ✅ | **本轮修复**——gap 8→5px |
| 头像尺寸/字号（18px/9px公式） | ✅ | **本轮修复**——Avatar组件字号公式重新拟合，18px 现在精确输出 9px（之前是 7.2px） |
| 姓名字号（10.5px） | ✅ | 之前就对，本轮核实无误 |
| 该收/该付金额字号（9.5px） | ✅ | 之前就对，本轮核实无误 |
| "查看XX的分摊明细"按钮（8.5px+margin-left 25px） | ✅ | **本轮修复**——从 padding-left:26px 改成字面一致的 margin-left:25px |
| 分摊明细展开面板（.settle-detail：浅底圆角盒子+行间border-top分隔） | ✅ | **本轮重做**——之前是"左侧竖线缩进"跟方案的"背景盒子+分隔线"完全是两套视觉语言，改成字面对齐；角色(垫付/分摊)/不计分摊标签是真实需要的信息，方案demo没有，塞进meta小字里保留 |
| "转账清单"小标题 | ✅ | round18 已统一 |
| 转账列表容器（同.list规格） | ✅ | **本轮修复**——之前跟净值列表用了不同的圆角/gap/padding，本轮统一成同一套 |
| 转账行（姓名字号10.5px/已收款提示9.5px/金额9.5px） | ✅ | **本轮修复**——之前整行统一11.5px没有区分层级 |
| 转账行头像 | ✅ | **round22 去掉**——Remy 明确表态"要"（照方案字面来），已删除，独立 ui-auditor 确认头像真的消失、净值列表的头像（不受影响的那部分）没被误伤、布局没有错位/留白怪异 |
| "标记已结算"按钮（.big-cta，未收齐时文案即状态） | ✅ | round19 已对齐 |

## 屏 05 · 支付方式

| 子区块 | 状态 | 说明 |
|---|---|---|
| 标题 | ✅ | 15px |
| 顶部说明文字（逐字方案原文） | ✅ | round18 已改 |
| "已配置的支付方式"列表 | ✅ | **本轮修复**——独立 ui-auditor 走查发现之前用的是 `.tx-item`/`rounded-xl`（12px圆角/行间gap 8px），跟 `#scr-payment .list`（14px圆角/gap 2px/padding 3px 5px）不是同一套。已改成共用 `.list` 容器 + `.p-row`(gap 5px)，`.nm` 10px/`.tag-note` 8.5px/`.del-icon` 10px+padding 2px 逐值对齐 |
| 删除图标（🗑非文字） | ✅ | round19 已改，本轮顺带修了字号/padding |
| "＋添加支付方式"按钮+展开表单 | ✅ | round17 已决定常驻展开，保留 |
| "本行程启用的支付方式"勾选列表 | ✅ | **本轮修复**——同上，从个体描边胶囊改成共用 `.list` 容器 + `.check-row`(10.5px) |
| "⚙设置当前余额"按钮+面板 | ✅ | round19 确认⚙符号本来就对（审计曾误判） |

## 屏 06 · 邀请管理

| 子区块 | 状态 | 说明 |
|---|---|---|
| 标题 | ✅ | 15px |
| 参与者列表（头像+姓名+邀请状态徽章） | ✅ | ui-auditor 核实真数据下 remy(已认领)/htoo(未认领) 两种状态都存在且渲染正确 |
| "现有邀请链接"区块 | ✅ | round17 已排到第一位 |
| "＋生成新邀请"按钮+弹层 | ✅ | ui-auditor 没有真的点开生成（会写入真实邀请码），改读代码确认 linkbox 字号是 `text-[9px]`，跟 `#scr-invites .linkbox{font-size:9px}` 一致 |
| "直接添加参与者"入口 | ✅ | 未改动 |
| "参与者认领状态"区块 | ✅ | 真实功能，方案demo没有，保留 |

## 屏 07 · 新建行程

| 子区块 | 状态 | 说明 |
|---|---|---|
| 标题 | ✅ | 15px |
| 行程名称字段 | ✅ | ui-auditor 核实样式跟其它输入框一致 |
| 主要币种字段说明文案 | ✅ | round17 已改方案原文 |
| 启用币种chip（顺序MYR/THB/USD/CNY/SGD/HKD） | ✅ | round17 已对齐前6个顺序 |
| 行程日期字段（合并成一个row2） | ✅ | round17 已合并 |
| "创建行程"按钮 | ✅ | .big-cta 全宽 |
| "你的称呼"+"同行人"字段 | ✅ | 真实功能，方案demo没有，保留 |

## 屏 08 · 我的账号

| 子区块 | 状态 | 说明 |
|---|---|---|
| "←返回"链接 | ✅ | ui-auditor 核实样式跟屏07一致 |
| 标题 | ✅ | 15px |
| 身份链接框+复制链接按钮 | ✅ | round17 已改全宽黑色.big-cta |
| "强制刷新最新版本"按钮 | ✅ | round14 刻意加的真功能，保留 |

## 屏 09 · 新建钱包（弹层）

| 子区块 | 状态 | 说明 |
|---|---|---|
| 弹层形式（真modal，非内联展开） | ✅ | round17 已改成真正弹窗+暗蒙层 |
| 钱包名字段 | ✅ | 未改动 |
| 币种字段 | ✅ | 未改动 |
| "绑定支付方式"下拉（永远渲染，非条件隐藏） | ✅ | round19 已修 |
| 账户类型选择（银行/现金/信用卡/手机支付图标） | ✅ | ui-auditor 实测4个图标可点选切换，"已选：银行"文字联动正确 |
| "建立钱包"按钮 | ✅ | .big-cta 全宽 |
| Esc键关闭 | ✅ | round19 已修 |

---

## round22 换汇完整流程实测（2026-09-17）

背景：round21 只测到"填表单但不提交"，理由是这条真实行程当前0个钱包、且换汇/钱包没有UI删除功能，怕留下删不掉的测试数据。这轮 Remy 明确要求"要真的走完全流程"，并指示测完用 `wrangler d1 execute` 直接清理数据库（沿用round15清理验证session的先例）。

**测试步骤（真实行程"🇭🇰2026香港"，真实账号，全程真机iPhone视口）**：
1. 新建钱包 B："验证-可删-round22钱包B(HKD)"，币种HKD（充值目标）。
2. 打开这张卡的"💱取款/换汇"面板，用"＋添加来源钱包"内联新建钱包 A："验证-可删-round22钱包A(MYR)"，币种MYR。
3. 填"拿出多少"=100（MYR），填"本次汇率"=1.6（1 MYR = 1.6 HKD），失焦后"换到多少"自动算出160.00——验证了 `applyRateToTotal` 这个真实联动逻辑。
4. 点"保存充值记录"提交。

**结果（直接查 D1 数据库确认，不是只看UI）**：
- `exchange_record` 表新增1条：`from_wallet_id`=钱包A、`to_wallet_id`=钱包B、`from_amount`=10000分（100.00 MYR）、`to_amount`=16000分（160.00 HKD）——跟填的数字完全对得上。
- `wallet` 表：钱包B `current_balance`=16000（0+16000，充值目标正确增加）；钱包A `current_balance`=-10000（0-10000，来源钱包正确扣减，负数是设计如此——新钱包起始余额固定0，来源钱包被扣成负数是预期行为，不是bug，往后可以用"设置当前余额"手动校正）。
- **双边钱包余额都正确更新，换汇记录正确写入，这条 Remy 反复问的功能这次是真的测到底了，不是只测了表单交互。**

**清理**（`wrangler d1 execute --remote`）：先删 `exchange_record` 那1行（FK依赖顺序，必须先删这个）→ `changes:1` 确认；再删 `wallet` 表2行（钱包A+B）→ `changes:2` 确认；最后 `SELECT count(*)` 精确核对：`wallets_left=0`、`exchanges_left=0`、`orphan_test_wallets_left=0`（按 label 前缀`验证-可删-round22`全库扫描，非本地where子句碰运气）。这条真实行程现在的钱包/换汇记录数量跟测试前完全一样，没有残留。

**顺带发现的真实产品缺口（Remy 要求如实记录）**：
- 读了 `app/api/trips/[tripId]/wallets/[walletId]/route.ts` 源码，**钱包后端其实是有 `DELETE` 端点的**（有关联换汇记录时会挡住返回409，没关联记录才允许删）——round17-21 反复写的"wallets表只有PATCH没有DELETE"这句话是不准确的，没人真的去查过这个路由文件的完整方法列表。真实情况是：**后端能力有，前端 `wallet-grid.tsx` 完全没有接删除按钮**，所以对 Remy 来说体验上确实是"没法删"，但要补这个功能只是加一个UI按钮调现成的API，不是要新写一整套后端删除逻辑，成本比想象的低。
- `exchange_record` 表**确认没有任何删除端点**（`app/api/trips/[tripId]/exchange-records/route.ts` 只有 GET/POST，没有 `[exchangeRecordId]` 子路由）——这条是真的完全没有删除能力，如果要加，是全新的后端工作。
- 这两条都还没做，只是这轮测试顺带查清楚了准确的现状，需不需要补UI删除按钮/新增删除端点，留给 Remy 判断优先级。

---

## round22 iOS 输入框缩放实测（2026-09-17）

用真实 Playwright 脚本（不是走查工具的截图观感，是精确到像素的 `getComputedStyle` 测量），iPhone 12 视口（390×844），真的点击聚焦每一个真实 `<input>`/`<textarea>`，读取实际渲染字号。覆盖字段：快速记账卡商家名称+自定义分摊4个金额框、记一笔消费全部字段（含备注textarea+自定义分摊2个金额框）、新建行程5个字段、支付方式5个字段、邀请管理3个字段（"生成新邀请"+"直接添加参与者"两个展开区都测了）、新建钱包弹层、**取款/换汇表单3个字段（拿出多少/本次汇率/换到多少，这轮换汇全流程实测顺带测的，之前0钱包测不到）**——共约28个字段，**修复后全部 = 16px**。

**过程中真的抓到一个bug**：第一版CSS只用了 `input[type=...]` 属性选择器+裸 `textarea` 标签选择器，`textarea` 单独一个标签选择器 specificity (0,0,1) 比不过 `.field-input` 的 (0,1,0)——"记一笔消费"备注框实测聚焦后还是10px，没生效。补了 `textarea.field-input`/`textarea.field-input-dark` 两条更具体的选择器后重新实测=16px。**这是"应该对"和"测出来才知道对不对"的真实区别，如果不实测直接报"已修复"会是一次假报告。**

**方法论局限如实说明**：这套测试量的是"触发iOS强制缩放的技术条件（字号<16px）是否解除"，不是"在真实iPhone Safari上肉眼看有没有跳出缩放动画"——Playwright的Chromium引擎能精确测字号但不能重现Safari那套缩放渲染行为，如果 Remy 自己拿真实iPhone点一下还是感觉到跳动，需要回来反馈进一步排查（但从字号这个决定性变量的角度，触发条件已经不在了）。

---

## 本轮（第二十二轮）已知遗留 / 需要 Remy 表态或留意的事项

1. ~~"复制链接"按钮反馈问题~~——**round24 已排查清楚，判定代码没问题**。自己写真实 Playwright 脚本（显式控制剪贴板权限，不是走查工具那套默认无权限的环境）分别测了"有权限"和"无权限"两种场景，各自 6+/8+ 次独立浏览器实例全部稳定复现"成功显示已复制"/"失败显示复制失败"，代码逻辑证明是对的。round21/22 的"没反应"最可能是①走查工具默认没给剪贴板权限 ②"已复制"只维持2秒，agent 点击到查看之间的延迟经常超过这个窗口，两条叠加导致看起来像"没反应"。详见 PENDING-DECISIONS 第二十四轮。
2. ~~钱包/换汇删除功能缺口~~——**round24 已做完**：钱包胶囊新增删除图标（接上既有DELETE端点，二次确认+余额/关联记录警告），换汇记录新增全套删除能力（新端点+前端删除入口+双边钱包余额自动回滚）。详见 PENDING-DECISIONS 第二十四轮。
3. **汇率比价渠道那组的点差/手续费百分比本身仍是参考值**（不是接口现查）——这轮改的只是"中间价"这一层变成实时，Wise/TNG跨境等各渠道的具体百分比（-0.7%/-1.5%等）还是写死的数字，如果以后要连这部分也做成可配置/查证，是另一件事。
4. 全部 9 屏的清单条目本轮再次逐条独立复核过一遍（不再有 🔲 未检查项，且这次是一个跟round21实现无关的全新独立 agent 重新核对，不是自证）。

---

## 第二十一轮追加：全功能点功能性 QA（2026-09-17）

Remy 追加要求"每一项功能都要测试，看有没有什么bug是还没解决的"——这是跟上面视觉对齐清单**完全独立**的一轮测试，专门抓"点了没反应/报错/算错/状态没更新"这类真实功能 bug，不是像素/字号。全程在真实行程"🇭🇰2026香港"上进行，用真实身份链接登录，A类功能（消费/支付方式/邀请）走了完整的建-测-删闭环，测完已确认清理干净；B类功能（标记已结算/新建钱包/直接添加参与者/创建新行程，这几个后端没有删除能力）只测到提交前一步，没有真的提交，没有留下任何删不掉的残留数据。

### 测试结果

| 功能 | 测试深度 | 结果 |
|---|---|---|
| 记一笔消费（各字段+split三档+编辑+删除） | 完整闭环 | ✅ 全部正常，约算金额/自定义分摊守恒/编辑生效都验证过 |
| 快速记账（分类下拉+币种+分摊+提交） | 完整闭环 | ✅ 正常，分类下拉bug没有复发 |
| 支付方式（新增+启用+删除） | 完整闭环 | ✅ 正常；"设置当前余额"因这条真实行程0钱包没测到底（非bug，是数据现状） |
| 邀请管理（生成+撤销+认领状态） | 完整闭环（撤销是正常操作路径） | ✅ 正常 |
| 汇率比价（目标币种/自选渠道/刷新） | 完整 | ⚠️ **发现真bug，已修复**——见下 |
| 结算（已收款勾选+标记已结算按钮状态） | 勾选完整测，最终确认按钮只测状态不点到底 | ✅ 正常 |
| 我的账号（复制链接+强制刷新） | 完整 | ⚠️ **发现UX轻微问题，已修复**——见下 |
| 活动流排序/筛选 | 完整 | ✅ 全部真实生效，不是摆设 |
| 新建钱包/直接添加参与者/创建新行程（B类保护） | 只测到提交前 | 表单交互正常，未提交，未产生数据 |
| 取款/换汇（0钱包场景） | 只测到"先建钱包"引导页 | ✅ 保护逻辑生效；完整提交流程未测到（见上方悬案第3条） |

### 本轮追加发现并修复的 2 个真实 bug

1. **汇率比价"我持有"候选池遗漏 CNY（中等严重度）**——这条真实行程 enabled_currencies 明确包含 CNY，但"💰我持有"下拉候选池固定读 `Object.keys(FX_RATES)`（只有 MYR/USD/HKD 三个基准），CNY 从来没被当过"我持有"的基准，即使行程明确启用了它；"🎯目标币种"那边反而能选到 CNY，两个下拉候选不对称。修法：在 `FX_RATES` 静态表补上 CNY 作为第四个基准（`app/trips/[tripId]/fx-compare-card.tsx`），用同一套"从 MYR 那行反推"方法算出 1 CNY≈0.6211 MYR 再换算出 THB/USD/SGD/HKD，数量级可追溯不是瞎编。
2. **"复制链接"按钮剪贴板权限被拒时完全静默、无任何反馈（轻微）**——`app/account/account-identity-link.tsx` + `app/trips/[tripId]/invites/invites-manager.tsx` 两处的 `handleCopy` 之前 catch 到剪贴板权限错误就什么都不做，用户点了不知道是成功还是没反应。已加失败提示文字"复制失败（浏览器不给剪贴板权限），可以手动选取上面的链接文字复制"，让失败分支也可见，跟成功分支（按钮变"已复制"）对称。

### 验证

**独立定向复核（跟做修复的不是同一个 agent）**：针对上面 2 个修复点 + 简单回归检查，用真实行程验证，全程只读未提交任何数据。
- CNY 候选：✅ 通过——"我持有"下拉正确出现 MYR/USD/HKD/CNY 四项，选中 CNY 后基准卡片+渠道列表数字都正常（无 NaN/报错），目标币种候选正确变成 THB/USD/SGD/HKD。
- 复制链接失败提示：✅ 通过——两处页面点击都能看到"已复制"反馈；复核过程中发现一个现象：Playwright 自动化浏览器里**每次刚导航到页面后第一次点击**"复制链接"完全没反应（不报错、不显示任何文字），必须点第二次才生效，两个页面都稳定复现2-3次。代码逻辑本身对称正确（成功/失败两条分支都会更新可见UI），怀疑是无头浏览器 Clipboard API 权限确认时序的自动化环境干扰，不一定是真实用户会遇到的问题——**如果 Remy 自己在真实浏览器里点一次也遇到"第一次点没反应"，需要回来反馈，那就是没修好；如果第一次点就正常，这条按环境干扰处理，不用管**。
- 回归检查：✅ 行程主页/结算页/记一笔消费页视觉正常，console 无新增报错。

**代码验证**：`./deploy.sh` 五关全过，线上 Version ID `56b69a4b-e4c3-4e7b-9695-c3903745d24d`。

---

*最后更新：2026-09-17，第二十二轮*
